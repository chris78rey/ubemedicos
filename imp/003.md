## ⚠️ Análisis de Impacto y Riesgos

**Módulos afectados:**

* `backend/apps/appointments/api_views.py`
* `backend/apps/appointments/tests_api.py`
* `web_frontend/pages/patient/appointments.vue`

**Riesgos detectados:**

* Existe una **inconsistencia funcional**: el backend del paciente cancela cualquier cita activa sin validar `is_paid`, mientras el backend del profesional sí bloquea la cancelación de citas pagadas.  
* La pantalla del paciente todavía deja visible el botón **Cancelar cita** para cualquier cita activa, aunque ya existe un mensaje que reconoce que las citas pagadas dependen del flujo administrativo.  
* Si no se corrige, el sistema seguirá permitiendo que el paciente cancele una cita pagada por un lado, mientras al profesional se le prohíbe por otro.  

**Supuestos:**

* **No** se desea cancelar automáticamente una cita pagada.
* La regla correcta es: **si la cita está pagada, la cancelación queda bloqueada hasta resolver reembolso o ajuste administrativo**.
* **No** se requieren migraciones ni cambios de base de datos.

---

## 🛠️ Preparación y Backups

**PowerShell**

```powershell
Copy-Item "backend\apps\appointments\api_views.py" "backend\apps\appointments\api_views.py.bak_paid_cancel_alignment"
Copy-Item "backend\apps\appointments\tests_api.py" "backend\apps\appointments\tests_api.py.bak_paid_cancel_alignment"
Copy-Item "web_frontend\pages\patient\appointments.vue" "web_frontend\pages\patient\appointments.vue.bak_paid_cancel_alignment"
```

**Validación previa**

```bash
cd backend
..\.venv\Scripts\python.exe manage.py check
```

---

## 🚀 Implementación por Fases

### Fase 1: Bloquear cancelación pagada en backend del paciente

**Objetivo:** alinear la regla del paciente con la del profesional.
**Ruta del Archivo:** `backend/apps/appointments/api_views.py`
**Acción:** **Reemplazar** la función `patient_appointment_cancel_view` por esta versión.

**Código:**

```python
# --- INICIO CAMBIO NUEVO ---
@require_http_methods(["POST"])
@api_roles_required("patient")
def patient_appointment_cancel_view(request, appointment_id: int):
    patient = request.api_user.patient_profile
    try:
        app = (
            Appointment.objects.select_related(
                "patient__user",
                "professional__user",
                "professional__specialty",
            )
            .get(id=appointment_id, patient=patient)
        )
    except Appointment.DoesNotExist:
        return _json_error("Cita no encontrada.", status=404)

    if app.status == Appointment.Status.CANCELLED_BY_PATIENT:
        return JsonResponse(_serialize_appointment(app), status=200)

    if app.status not in [
        Appointment.Status.PENDING_CONFIRMATION,
        Appointment.Status.CONFIRMED,
    ]:
        return _json_error("Solo se pueden cancelar citas activas.", status=409)

    if app.is_paid:
        return _json_error(
            "No se puede cancelar una cita pagada desde este flujo mínimo. "
            "Primero debe resolverse el reembolso o ajuste administrativo.",
            status=409,
        )

    with transaction.atomic():
        app = Appointment.objects.select_for_update().get(id=app.id)
        before_status = app.status
        app.status = Appointment.Status.CANCELLED_BY_PATIENT
        app.save(update_fields=["status"])

        AuditEvent.objects.create(
            actor=request.api_user,
            event_type="appointment_cancelled_by_patient",
            entity_type="Appointment",
            entity_id=str(app.id),
            metadata={
                "before_status": before_status,
                "after_status": app.status,
                "is_paid": app.is_paid,
            },
        )

    app.refresh_from_db()
    app = (
        Appointment.objects.select_related(
            "patient__user",
            "professional__user",
            "professional__specialty",
        )
        .get(id=app.id)
    )
    return JsonResponse(_serialize_appointment(app), status=200)
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 1:**

* Una cita activa **no pagada** debe cancelarse con `200`.
* Una cita activa **pagada** debe responder `409`.

**Rollback Fase 1:**

```powershell
Copy-Item "backend\apps\appointments\api_views.py.bak_paid_cancel_alignment" "backend\apps\appointments\api_views.py" -Force
```

---

### Fase 2: Blindar con pruebas automáticas

**Objetivo:** evitar que esta inconsistencia reaparezca.
**Ruta del Archivo:** `backend/apps/appointments/tests_api.py`
**Acción:** **Reemplazar** el método `test_cancel_appointment` y **agregar** un nuevo método justo debajo.

**Código:**

```python
# --- INICIO CAMBIO NUEVO ---
    def test_cancel_appointment(self):
        dt = timezone.make_aware(
            timezone.datetime.combine(
                self.next_monday,
                timezone.datetime.strptime("10:00", "%H:%M").time(),
            )
        )
        app = Appointment.objects.create(
            patient=self.pat_profile,
            professional=self.prof_profile,
            scheduled_at=dt,
            ends_at=dt + timedelta(minutes=30),
            modality="in_person",
            status=Appointment.Status.CONFIRMED,
            price=50.00,
            is_paid=False,
        )

        response = self.client.post(
            f"/api/v1/patient/appointments/{app.id}/cancel",
            **self._auth_headers(self.pat_token)
        )
        self.assertEqual(response.status_code, 200)

        app.refresh_from_db()
        self.assertEqual(app.status, Appointment.Status.CANCELLED_BY_PATIENT)

    def test_cannot_cancel_paid_appointment(self):
        dt = timezone.make_aware(
            timezone.datetime.combine(
                self.next_monday,
                timezone.datetime.strptime("10:30", "%H:%M").time(),
            )
        )
        app = Appointment.objects.create(
            patient=self.pat_profile,
            professional=self.prof_profile,
            scheduled_at=dt,
            ends_at=dt + timedelta(minutes=30),
            modality="in_person",
            status=Appointment.Status.CONFIRMED,
            price=50.00,
            is_paid=True,
        )

        response = self.client.post(
            f"/api/v1/patient/appointments/{app.id}/cancel",
            **self._auth_headers(self.pat_token)
        )
        self.assertEqual(response.status_code, 409)

        app.refresh_from_db()
        self.assertEqual(app.status, Appointment.Status.CONFIRMED)
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 2:**

```bash
cd backend
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api
```

**Rollback Fase 2:**

```powershell
Copy-Item "backend\apps\appointments\tests_api.py.bak_paid_cancel_alignment" "backend\apps\appointments\tests_api.py" -Force
```

---

### Fase 3: Ocultar botón de cancelar en la web del paciente cuando la cita esté pagada

**Objetivo:** alinear la interfaz con la regla real del backend.
**Ruta del Archivo:** `web_frontend/pages/patient/appointments.vue`
**Acción:** **Reemplazar** la función `canCancel`.

**Código:**

```ts
// --- INICIO CAMBIO NUEVO ---
function canCancel(appointment: AppointmentItem) {
  return (
    ['pending_confirmation', 'confirmed'].includes(appointment.status) &&
    !appointment.is_paid
  )
}
// --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 3:**

* Si `appointment.is_paid === false`, el botón **Cancelar cita** debe mostrarse.
* Si `appointment.is_paid === true`, el botón **Cancelar cita** no debe mostrarse.
* El texto informativo ya existente debe seguir visible en citas pagadas. 

**Rollback Fase 3:**

```powershell
Copy-Item "web_frontend\pages\patient\appointments.vue.bak_paid_cancel_alignment" "web_frontend\pages\patient\appointments.vue" -Force
```

---

## 🧪 Pruebas de Regresión

Ejecutar estas pruebas:

```bash
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api_lifecycle
```

Luego validar manualmente:

```bash
cd web_frontend
npm run dev
```

Validaciones manuales:

* Paciente con cita activa **no pagada**: debe poder cancelar.
* Paciente con cita activa **pagada**: no debe ver botón cancelar.
* Si se fuerza la llamada manual por API para una cita pagada, debe responder `409`.
* Profesional debe seguir sin poder cancelar citas pagadas, exactamente como ya estaba. 
* El flujo de pago no debe alterarse: una cita pagada sigue indicando resolución administrativa en la UI del paciente. 

---

## 📉 Plan de Reversión Total

**Paso 1: restaurar archivos**

```powershell
Copy-Item "backend\apps\appointments\api_views.py.bak_paid_cancel_alignment" "backend\apps\appointments\api_views.py" -Force
Copy-Item "backend\apps\appointments\tests_api.py.bak_paid_cancel_alignment" "backend\apps\appointments\tests_api.py" -Force
Copy-Item "web_frontend\pages\patient\appointments.vue.bak_paid_cancel_alignment" "web_frontend\pages\patient\appointments.vue" -Force
```

**Paso 2: validar integridad**

```bash
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api
```

**Paso 3: levantar frontend**

```bash
cd web_frontend
npm run dev
```

El siguiente paso natural después de este sería **crear un flujo administrativo de cancelación + reembolso + cambio de estado de cita en una sola operación**, para que el admin no tenga que ejecutar acciones separadas.


---



Se propone implementar **la resolución administrativa unificada de cita pagada**: **reembolso + cancelación de la cita en una sola operación atómica**, sin migraciones nuevas. El snapshot actual ya tiene pagos con estado `refunded`, pero la API administrativa visible solo expone `mark-succeeded` y `mark-failed`; además no existe ruta administrativa propia para citas, y el modelo de citas solo contempla cancelación **por paciente** o **por profesional**. Por eso la salida más segura es reutilizar esos estados existentes mediante un parámetro `cancel_as`, en vez de inventar un nuevo estado o tocar base de datos.    

## ⚠️ Análisis de Impacto y Riesgos

**Módulos afectados:**

* `backend/apps/payments/api_views.py`
* `backend/apps/payments/api_urls.py`
* `backend/apps/payments/tests_api.py`
* `web_frontend/pages/admin/payments.vue`

**Riesgos detectados:**

* Si se separa reembolso y cancelación en dos acciones distintas, pueden quedar pagos reembolsados con cita aún activa.
* Si se cancela una cita cerrada (`completed`, `no_show_*`), se rompe la trazabilidad clínica.
* Si se usa un estado nuevo de cita, obligaría a migración y a tocar más pantallas.

**Supuestos:**

* El admin podrá decidir si la cancelación debe quedar como `cancelled_by_patient` o `cancelled_by_professional`.
* El pago reembolsado **conserva** `paid_at`; lo que cambia es el estado a `refunded`.
* No se añadirá tabla nueva ni columna nueva.

---

## 🛠️ Preparación y Backups

```powershell
Copy-Item "backend\apps\payments\api_views.py" "backend\apps\payments\api_views.py.bak_admin_resolve_paid"
Copy-Item "backend\apps\payments\api_urls.py" "backend\apps\payments\api_urls.py.bak_admin_resolve_paid"
Copy-Item "backend\apps\payments\tests_api.py" "backend\apps\payments\tests_api.py.bak_admin_resolve_paid"
Copy-Item "web_frontend\pages\admin\payments.vue" "web_frontend\pages\admin\payments.vue.bak_admin_resolve_paid"
```

```bash
cd backend
..\.venv\Scripts\python.exe manage.py check
```

---

## 🚀 Implementación por Fases

### Fase 1: Agregar endpoint administrativo unificado

**Objetivo:** resolver en una sola llamada el caso de cita pagada que debe quedar reembolsada y cancelada.
**Ruta del Archivo:** `backend/apps/payments/api_views.py`
**Acción:** **Insertar** un mapa nuevo y una vista nueva.

### 1A. Insertar este bloque **debajo de** `TERMINAL_APPOINTMENT_STATUSES`

```python
# --- INICIO CAMBIO NUEVO ---
ADMIN_CANCELLATION_TARGET_MAP = {
    "patient": Appointment.Status.CANCELLED_BY_PATIENT,
    "professional": Appointment.Status.CANCELLED_BY_PROFESSIONAL,
}
# --- FIN CAMBIO NUEVO ---
```

### 1B. Insertar esta función **después de** `admin_payment_mark_failed_view`

```python
# --- INICIO CAMBIO NUEVO ---
@require_http_methods(["POST"])
@api_roles_required("admin", "super_admin")
def admin_payment_refund_and_cancel_appointment_view(request, payment_id: int):
    try:
        body = _parse_json_body(request)
    except ValueError as exc:
        return _json_error(str(exc))

    payment = _get_payment_with_related(payment_id)
    if not payment:
        return _json_error("Pago no encontrado.", status=404)

    cancel_as = (body.get("cancel_as") or "").strip().lower()
    if cancel_as not in ADMIN_CANCELLATION_TARGET_MAP:
        return _json_error(
            "cancel_as debe ser 'patient' o 'professional'.",
            status=400,
        )

    target_status = ADMIN_CANCELLATION_TARGET_MAP[cancel_as]
    admin_notes = (body.get("notes") or "").strip()
    appointment = payment.appointment

    if payment.status not in {Payment.Status.SUCCEEDED, Payment.Status.REFUNDED}:
        return _json_error(
            "Solo se puede resolver este flujo sobre pagos exitosos o ya reembolsados.",
            status=409,
        )

    if appointment.status in {
        Appointment.Status.COMPLETED,
        Appointment.Status.NO_SHOW_PATIENT,
        Appointment.Status.NO_SHOW_PROFESSIONAL,
    }:
        return _json_error(
            "No se puede cancelar una cita ya cerrada.",
            status=409,
        )

    if (
        payment.status == Payment.Status.REFUNDED
        and appointment.status == target_status
        and not appointment.is_paid
    ):
        return JsonResponse(_serialize_payment(payment, include_raw=True), status=200)

    if appointment.status in {
        Appointment.Status.CANCELLED_BY_PATIENT,
        Appointment.Status.CANCELLED_BY_PROFESSIONAL,
    } and appointment.status != target_status:
        return _json_error(
            "La cita ya fue cancelada con otro origen de cancelación.",
            status=409,
        )

    with transaction.atomic():
        payment = Payment.objects.select_for_update().get(id=payment.id)
        appointment = Appointment.objects.select_for_update().get(id=payment.appointment_id)

        before_payment_status = payment.status
        before_appointment_status = appointment.status

        payment.status = Payment.Status.REFUNDED
        payment.raw_response = {
            **(payment.raw_response or {}),
            "provider": "demo_manual",
            "decision": "refund_and_cancel_appointment",
            "cancel_as": cancel_as,
            "admin_notes": admin_notes,
            "refunded_at": timezone.now().isoformat(),
            "appointment_status_after": target_status,
            "updated_at": timezone.now().isoformat(),
            "updated_by": request.api_user.email,
        }
        payment.save(update_fields=["status", "raw_response"])

        appointment.status = target_status
        appointment.is_paid = False
        appointment.save(update_fields=["status", "is_paid"])

        AuditEvent.objects.create(
            actor=request.api_user,
            event_type="payment_refunded_and_appointment_cancelled",
            entity_type="Payment",
            entity_id=str(payment.id),
            metadata={
                "appointment_id": appointment.id,
                "before_payment_status": before_payment_status,
                "after_payment_status": payment.status,
                "before_appointment_status": before_appointment_status,
                "after_appointment_status": appointment.status,
                "cancel_as": cancel_as,
                "is_paid": appointment.is_paid,
            },
        )

        AuditEvent.objects.create(
            actor=request.api_user,
            event_type="appointment_cancelled_after_payment_refund",
            entity_type="Appointment",
            entity_id=str(appointment.id),
            metadata={
                "payment_id": payment.id,
                "before_status": before_appointment_status,
                "after_status": appointment.status,
                "cancel_as": cancel_as,
                "payment_status": payment.status,
                "is_paid": appointment.is_paid,
            },
        )

    payment = _get_payment_with_related(payment.id)
    return JsonResponse(_serialize_payment(payment, include_raw=True), status=200)
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 1:**
Debe aceptar `POST /api/v1/admin/payments/{id}/refund-and-cancel-appointment` con JSON:

```json
{
  "cancel_as": "patient",
  "notes": "Solicitud administrativa"
}
```

**Rollback Fase 1**

```powershell
Copy-Item "backend\apps\payments\api_views.py.bak_admin_resolve_paid" "backend\apps\payments\api_views.py" -Force
```

---

### Fase 2: Exponer la ruta nueva

**Objetivo:** publicar la operación unificada en la API.
**Ruta del Archivo:** `backend/apps/payments/api_urls.py`
**Acción:** **Reemplazar archivo completo**

```python
# --- INICIO CAMBIO NUEVO ---
from django.urls import path

from .api_views import (
    patient_payments_collection_view,
    patient_appointment_payment_intent_view,
    admin_payments_collection_view,
    admin_payment_mark_succeeded_view,
    admin_payment_mark_failed_view,
    admin_payment_refund_and_cancel_appointment_view,
)

urlpatterns = [
    # --- Patient ---
    path(
        "patient/payments",
        patient_payments_collection_view,
        name="patient-payments-collection",
    ),
    path(
        "patient/appointments/<int:appointment_id>/payment-intent",
        patient_appointment_payment_intent_view,
        name="patient-appointment-payment-intent",
    ),
    # --- Admin ---
    path(
        "admin/payments",
        admin_payments_collection_view,
        name="admin-payments-collection",
    ),
    path(
        "admin/payments/<int:payment_id>/mark-succeeded",
        admin_payment_mark_succeeded_view,
        name="admin-payment-mark-succeeded",
    ),
    path(
        "admin/payments/<int:payment_id>/mark-failed",
        admin_payment_mark_failed_view,
        name="admin-payment-mark-failed",
    ),
    path(
        "admin/payments/<int:payment_id>/refund-and-cancel-appointment",
        admin_payment_refund_and_cancel_appointment_view,
        name="admin-payment-refund-and-cancel-appointment",
    ),
]
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 2:**
La ruta nueva debe quedar visible junto a las ya existentes.

**Rollback Fase 2**

```powershell
Copy-Item "backend\apps\payments\api_urls.py.bak_admin_resolve_paid" "backend\apps\payments\api_urls.py" -Force
```

---

### Fase 3: Blindar con pruebas automáticas

**Objetivo:** impedir regresiones.
**Ruta del Archivo:** `backend/apps/payments/tests_api.py`
**Acción:** **Agregar estos métodos al final de la clase** `PaymentDemoApiTests`

```python
# --- INICIO CAMBIO NUEVO ---
    def test_admin_refund_and_cancel_paid_appointment_as_patient(self):
        payment = Payment.objects.create(
            appointment=self.appointment,
            external_reference="demo_reference_020",
            amount=self.appointment.price,
            currency="USD",
            status=Payment.Status.SUCCEEDED,
            paid_at=timezone.now(),
        )

        self.appointment.status = Appointment.Status.CONFIRMED
        self.appointment.is_paid = True
        self.appointment.save(update_fields=["status", "is_paid"])

        response = self.client.post(
            f"/api/v1/admin/payments/{payment.id}/refund-and-cancel-appointment",
            data=json.dumps({
                "cancel_as": "patient",
                "notes": "Resolución administrativa unificada",
            }),
            content_type="application/json",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)

        payment.refresh_from_db()
        self.appointment.refresh_from_db()

        self.assertEqual(payment.status, Payment.Status.REFUNDED)
        self.assertEqual(
            self.appointment.status,
            Appointment.Status.CANCELLED_BY_PATIENT,
        )
        self.assertFalse(self.appointment.is_paid)

    def test_admin_refund_and_cancel_paid_appointment_as_professional(self):
        payment = Payment.objects.create(
            appointment=self.appointment,
            external_reference="demo_reference_021",
            amount=self.appointment.price,
            currency="USD",
            status=Payment.Status.SUCCEEDED,
            paid_at=timezone.now(),
        )

        self.appointment.status = Appointment.Status.CONFIRMED
        self.appointment.is_paid = True
        self.appointment.save(update_fields=["status", "is_paid"])

        response = self.client.post(
            f"/api/v1/admin/payments/{payment.id}/refund-and-cancel-appointment",
            data=json.dumps({
                "cancel_as": "professional",
                "notes": "Resolución administrativa por agenda",
            }),
            content_type="application/json",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)

        payment.refresh_from_db()
        self.appointment.refresh_from_db()

        self.assertEqual(payment.status, Payment.Status.REFUNDED)
        self.assertEqual(
            self.appointment.status,
            Appointment.Status.CANCELLED_BY_PROFESSIONAL,
        )
        self.assertFalse(self.appointment.is_paid)

    def test_admin_cannot_refund_and_cancel_pending_payment(self):
        payment = Payment.objects.create(
            appointment=self.appointment,
            external_reference="demo_reference_022",
            amount=self.appointment.price,
            currency="USD",
            status=Payment.Status.PENDING,
        )

        response = self.client.post(
            f"/api/v1/admin/payments/{payment.id}/refund-and-cancel-appointment",
            data=json.dumps({
                "cancel_as": "patient",
                "notes": "No debe permitir",
            }),
            content_type="application/json",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 409)

        payment.refresh_from_db()
        self.appointment.refresh_from_db()

        self.assertEqual(payment.status, Payment.Status.PENDING)
        self.assertEqual(self.appointment.status, Appointment.Status.PENDING_CONFIRMATION)

    def test_admin_cannot_refund_and_cancel_completed_appointment(self):
        payment = Payment.objects.create(
            appointment=self.appointment,
            external_reference="demo_reference_023",
            amount=self.appointment.price,
            currency="USD",
            status=Payment.Status.SUCCEEDED,
            paid_at=timezone.now(),
        )

        self.appointment.status = Appointment.Status.COMPLETED
        self.appointment.is_paid = True
        self.appointment.save(update_fields=["status", "is_paid"])

        response = self.client.post(
            f"/api/v1/admin/payments/{payment.id}/refund-and-cancel-appointment",
            data=json.dumps({
                "cancel_as": "patient",
                "notes": "No debe permitir cita cerrada",
            }),
            content_type="application/json",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 409)

        payment.refresh_from_db()
        self.appointment.refresh_from_db()

        self.assertEqual(payment.status, Payment.Status.SUCCEEDED)
        self.assertEqual(self.appointment.status, Appointment.Status.COMPLETED)
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 3**

```bash
cd backend
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
```

**Rollback Fase 3**

```powershell
Copy-Item "backend\apps\payments\tests_api.py.bak_admin_resolve_paid" "backend\apps\payments\tests_api.py" -Force
```

---

### Fase 4: Exponer la acción en el panel admin de pagos

**Objetivo:** permitir que el admin resuelva la cita pagada sin ir a otra pantalla.
**Ruta del Archivo:** `web_frontend/pages/admin/payments.vue`
**Acción:** hacer estos reemplazos exactos.

#### 4A. Debajo de `const notesByPayment = reactive<Record<number, string>>({})`

```ts
// --- INICIO CAMBIO NUEVO ---
const cancelAsByPayment = reactive<Record<number, '' | 'patient' | 'professional'>>({})

const cancelOriginOptions = [
  { title: 'Cancelar como paciente', value: 'patient' },
  { title: 'Cancelar como profesional', value: 'professional' },
]
// --- FIN CAMBIO NUEVO ---
```

#### 4B. Debajo de `function canMarkFailed(payment: PaymentItem) { ... }`

```ts
// --- INICIO CAMBIO NUEVO ---
function canResolvePaidAppointment(payment: PaymentItem) {
  if (!['succeeded', 'refunded'].includes(payment.status)) {
    return false
  }

  if (['completed', 'no_show_patient', 'no_show_professional'].includes(payment.appointment.status)) {
    return false
  }

  if (
    payment.status === 'refunded' &&
    !payment.appointment.is_paid &&
    ['cancelled_by_patient', 'cancelled_by_professional'].includes(payment.appointment.status)
  ) {
    return false
  }

  return true
}

function resolvePaidAppointmentButtonText(payment: PaymentItem) {
  if (payment.status === 'refunded') {
    return 'Cancelar cita ya reembolsada'
  }
  return 'Reembolsar + cancelar cita'
}
// --- FIN CAMBIO NUEVO ---
```

#### 4C. Reemplazar la firma de `runAction`

**Buscar**

```ts
async function runAction(
  payment: PaymentItem,
  action: 'mark-succeeded' | 'mark-failed'
) {
```

**Reemplazar por**

```ts
async function runAction(
  payment: PaymentItem,
  action: 'mark-succeeded' | 'mark-failed' | 'refund-and-cancel-appointment'
) {
```

#### 4D. Dentro de `runAction`, **antes** del `try {`, insertar:

```ts
// --- INICIO CAMBIO NUEVO ---
  if (action === 'refund-and-cancel-appointment' && !cancelAsByPayment[payment.id]) {
    acting.value = false
    actingPaymentId.value = null
    errorMessage.value = 'Debe elegirse si la cancelación queda como paciente o como profesional.'
    return
  }
// --- FIN CAMBIO NUEVO ---
```

#### 4E. Dentro de `runAction`, reemplazar el bloque `body: { ... }`

**Buscar**

```ts
      body: {
        notes: notesByPayment[payment.id] || '',
      },
```

**Reemplazar por**

```ts
      body: {
        notes: notesByPayment[payment.id] || '',
        cancel_as: action === 'refund-and-cancel-appointment'
          ? cancelAsByPayment[payment.id]
          : undefined,
      },
```

#### 4F. Dentro de `runAction`, reemplazar `successMessage.value = ...`

**Buscar**

```ts
    successMessage.value =
      action === 'mark-succeeded'
        ? 'Pago marcado como exitoso correctamente.'
        : 'Pago marcado como fallido correctamente.'
```

**Reemplazar por**

```ts
    successMessage.value =
      action === 'mark-succeeded'
        ? 'Pago marcado como exitoso correctamente.'
        : action === 'mark-failed'
          ? 'Pago marcado como fallido correctamente.'
          : 'Pago y cita resueltos correctamente.'
```

#### 4G. Reemplazar el texto descriptivo superior

**Buscar**

```vue
<p class="text-medium-emphasis">
  Bandeja para revisar pagos demo y decidir manualmente si se marcan como exitosos o fallidos.
</p>
```

**Reemplazar por**

```vue
<p class="text-medium-emphasis">
  Bandeja para revisar pagos demo y resolver también citas pagadas en una sola operación administrativa.
</p>
```

#### 4H. Debajo del `<v-textarea ... />`, insertar:

```vue
<!-- --- INICIO CAMBIO NUEVO --- -->
            <v-select
              v-if="canResolvePaidAppointment(payment)"
              v-model="cancelAsByPayment[payment.id]"
              :items="cancelOriginOptions"
              item-title="title"
              item-value="value"
              label="Origen de cancelación para la resolución administrativa"
              density="comfortable"
              clearable
              class="mt-4"
            />
<!-- --- FIN CAMBIO NUEVO --- -->
```

#### 4I. Dentro de `<v-card-actions class="flex-wrap ga-2">`, insertar este botón **después** de “Marcar fallido”

```vue
<!-- --- INICIO CAMBIO NUEVO --- -->
            <v-btn
              color="info"
              variant="flat"
              :disabled="!canResolvePaidAppointment(payment)"
              :loading="acting && actingPaymentId === payment.id"
              @click="runAction(payment, 'refund-and-cancel-appointment')"
            >
              {{ resolvePaidAppointmentButtonText(payment) }}
            </v-btn>
<!-- --- FIN CAMBIO NUEVO --- -->
```

**Prueba de Fase 4:**
En `/admin/payments`, para un pago `succeeded`, debe poder elegirse `Cancelar como paciente` o `Cancelar como profesional` y ejecutar una sola acción.

**Rollback Fase 4**

```powershell
Copy-Item "web_frontend\pages\admin\payments.vue.bak_admin_resolve_paid" "web_frontend\pages\admin\payments.vue" -Force
```

---

## 🧪 Pruebas de Regresión

```bash
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api_lifecycle
```

Validación manual:

1. Pago `succeeded` + cita `confirmed` + `cancel_as=patient` → pago `refunded`, cita `cancelled_by_patient`, `is_paid=false`.
2. Pago `succeeded` + cita `confirmed` + `cancel_as=professional` → pago `refunded`, cita `cancelled_by_professional`, `is_paid=false`.
3. Pago `pending` no debe permitir esa operación.
4. Cita `completed` no debe permitir esa operación.
5. La bandeja admin debe seguir cargando normalmente, porque ya existe como panel operativo central para pagos. 

---

## 📉 Plan de Reversión Total

```powershell
Copy-Item "backend\apps\payments\api_views.py.bak_admin_resolve_paid" "backend\apps\payments\api_views.py" -Force
Copy-Item "backend\apps\payments\api_urls.py.bak_admin_resolve_paid" "backend\apps\payments\api_urls.py" -Force
Copy-Item "backend\apps\payments\tests_api.py.bak_admin_resolve_paid" "backend\apps\payments\tests_api.py" -Force
Copy-Item "web_frontend\pages\admin\payments.vue.bak_admin_resolve_paid" "web_frontend\pages\admin\payments.vue" -Force
```

```bash
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
```

La siguiente fase natural después de esta sería **mostrar en la pantalla del paciente una leyenda más precisa cuando la cancelación haya sido resuelta administrativamente con reembolso**, para que el historial quede completamente entendible desde la web.
