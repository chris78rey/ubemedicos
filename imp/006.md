#### ⚠️ Análisis de Impacto y Riesgos

**Módulos afectados:** `web_frontend/pages/login.vue` y flujo de autenticación contra `backend/apps/users/api_views.py` y `backend/apps/users/api_auth.py`. El backend ya expone `POST /api/v1/auth/login`, devuelve `access_token`, `token_type` y `user.primary_role`, y también acepta token por cookie `access_token`. 

**Riesgos detectados:** existe una diferencia entre lo pedido y lo que realmente devuelve el backend para administradores normales: el backend usa `primary_role = "admin_validation"` para `admin`, no `"admin"`. Por eso el frontend debe contemplar ambos valores para no dejar al usuario sin redirección. Además, el backend responde errores bajo `detail`, por lo que el manejo debe leer `error?.data?.detail`.

**Supuestos:** el proyecto ya tiene Vuetify activo, `useRuntimeConfig()` disponible en Nuxt 3, y la ruta `/register` existe o se implementará después. En el snapshot actual no aparece `web_frontend/pages/login.vue`, por lo que este archivo se debe crear nuevo. El estilo y la forma de consumir `apiBaseUrl` siguen el patrón usado en las otras páginas del frontend.

#### 🛠️ Preparación y Backups

**Ruta del archivo a crear:** `web_frontend/pages/login.vue`

**Backup previo si ya existe archivo localmente:**

```bash
copy web_frontend\pages\login.vue web_frontend\pages\login.vue.bak
```

#### 🚀 Implementación por Fases

**Fase 1: Crear la pantalla de login**
**Objetivo:** dejar una pantalla funcional, centrada, responsiva, con email, contraseña, toggle de visibilidad, validación mínima, manejo de errores y redirección por rol.

**Ruta del Archivo:** `web_frontend/pages/login.vue`
**Acción:** Crear

**Código:**

```vue
<!-- --- INICIO CAMBIO NUEVO --- -->
<script setup lang="ts">
type LoginResponse = {
  access_token: string
  token_type: string
  user?: {
    primary_role?: string | null
    email?: string
    first_name?: string
    last_name?: string
    [key: string]: any
  }
}

const runtimeConfig = useRuntimeConfig()

const apiBase = computed(() => {
  return runtimeConfig.public.apiBaseUrl || 'http://127.0.0.1:8000/api/v1'
})

const token = useCookie<string | null>('access_token', {
  path: '/',
})

const form = reactive({
  email: '',
  password: '',
})

const loading = ref(false)
const errorMessage = ref('')
const showPassword = ref(false)

const canSubmit = computed(() => {
  return form.email.trim().length > 0 && form.password.trim().length > 0
})

function resolveHomeByRole(primaryRole?: string | null) {
  const role = String(primaryRole || '').trim().toLowerCase()

  if (role === 'super_admin') {
    return '/admin'
  }

  if (
    role === 'admin' ||
    role === 'admin_validation' ||
    role === 'admin_support' ||
    role === 'admin_moderation' ||
    role === 'admin_privacy'
  ) {
    return '/admin'
  }

  if (role === 'privacy_auditor') {
    return '/admin/privacy/access-logs'
  }

  if (role === 'professional') {
    return '/professional'
  }

  if (role === 'patient') {
    return '/patient/appointments'
  }

  return '/'
}

async function handleLogin() {
  errorMessage.value = ''

  if (!canSubmit.value) {
    errorMessage.value = 'Email y contraseña son obligatorios.'
    return
  }

  loading.value = true

  try {
    const response = await $fetch<LoginResponse>(`${apiBase.value}/auth/login`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: {
        email: form.email.trim(),
        password: form.password,
      },
    })

    if (!response?.access_token) {
      throw new Error('No se recibió access_token en la respuesta.')
    }

    token.value = response.access_token

    const targetPath = resolveHomeByRole(response?.user?.primary_role)
    await navigateTo(targetPath)
  } catch (error: any) {
    errorMessage.value =
      error?.data?.detail ||
      'Credenciales inválidas o error de conexión'
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <v-container fluid class="login-page d-flex align-center justify-center">
    <v-row justify="center" class="w-100 ma-0">
      <v-col cols="12" sm="10" md="6" lg="4" xl="3">
        <v-card class="login-card" elevation="10" rounded="xl">
          <v-card-text class="pa-6 pa-md-8">
            <div class="text-center mb-6">
              <div class="text-h4 font-weight-bold mb-2">
                Ubemedicos
              </div>
              <div class="text-body-1 text-medium-emphasis">
                Inicio de sesión seguro
              </div>
            </div>

            <v-alert
              v-if="errorMessage"
              type="error"
              variant="tonal"
              class="mb-4"
            >
              {{ errorMessage }}
            </v-alert>

            <v-form @submit.prevent="handleLogin">
              <v-text-field
                v-model="form.email"
                label="Correo electrónico"
                type="email"
                autocomplete="email"
                variant="outlined"
                density="comfortable"
                prepend-inner-icon="mdi-email-outline"
                class="mb-3"
                :disabled="loading"
                clearable
              />

              <v-text-field
                v-model="form.password"
                label="Contraseña"
                :type="showPassword ? 'text' : 'password'"
                autocomplete="current-password"
                variant="outlined"
                density="comfortable"
                prepend-inner-icon="mdi-lock-outline"
                :append-inner-icon="showPassword ? 'mdi-eye-off-outline' : 'mdi-eye-outline'"
                class="mb-2"
                :disabled="loading"
                @click:append-inner="showPassword = !showPassword"
              />

              <v-btn
                type="submit"
                color="primary"
                block
                size="large"
                class="mt-4"
                :loading="loading"
                :disabled="loading || !canSubmit"
              >
                Iniciar sesión
              </v-btn>
            </v-form>

            <div class="text-center mt-6">
              <span class="text-body-2 text-medium-emphasis">
                ¿No tiene cuenta?
              </span>
              <NuxtLink to="/register" class="register-link ms-1">
                Registrarse
              </NuxtLink>
            </div>
          </v-card-text>
        </v-card>
      </v-col>
    </v-row>
  </v-container>
</template>

<style scoped>
.login-page {
  min-height: 100vh;
  background:
    linear-gradient(135deg, rgba(var(--v-theme-primary), 0.08), rgba(var(--v-theme-surface), 1));
  padding: 24px;
}

.login-card {
  width: 100%;
  max-width: 460px;
  margin: 0 auto;
  backdrop-filter: blur(4px);
}

.register-link {
  color: rgb(var(--v-theme-primary));
  text-decoration: none;
  font-weight: 600;
}

.register-link:hover {
  text-decoration: underline;
}
</style>
<!-- --- FIN CAMBIO NUEVO --- -->
```

**Prueba de Fase 1:** abrir `/login`, ingresar un usuario válido y verificar que el token quede en la cookie `access_token` y que la navegación ocurra según `user.primary_role`. El backend realmente devuelve `primary_role`, `role_codes` y `access_token`, por lo que el flujo queda alineado con la API existente.

**Rollback Fase 1:**

```bash
del web_frontend\pages\login.vue
```

#### 🧪 Pruebas de Regresión

Se debe probar lo siguiente después de pegar el archivo:

1. Login con `superadmin@demo.local / Admin123!` y validar redirección a `/admin`.
2. Login con `admin@demo.local / Admin123!` y validar redirección a `/admin`, aunque el backend devuelva `primary_role = admin_validation`. 
3. Login con `privacy@demo.local / Privacy123!` y validar redirección a `/admin/privacy/access-logs`.
4. Login con `profesional@demo.local / Profesional123!` y validar redirección a `/professional`.
5. Login con `paciente@demo.local / Paciente123!` y validar redirección a `/patient/appointments`. Los usuarios demo existen en el comando de seed. 
6. Intentar enviar vacío y verificar que no salga request al backend.
7. Intentar credenciales erróneas y verificar mensaje `Credenciales inválidas.` proveniente de `detail`. 

#### 📉 Plan de Reversión Total

1. Restaurar el backup:

```bash
copy /Y web_frontend\pages\login.vue.bak web_frontend\pages\login.vue
```

2. Si el archivo no existía antes:

```bash
del web_frontend\pages\login.vue
```

3. Reiniciar únicamente el frontend de Nuxt para volver al estado anterior.

**Nota operativa importante:** el backend actual no devuelve `primary_role = "admin"` para el rol `admin`; devuelve `admin_validation`. Por eso el código entregado ya viene blindado para ambos casos y evita una caída funcional en producción local.
