#### Impacto y riesgos

Sí se puede dejar la **Fase 1** en modo copiar y pegar, pero solo para el bloque correcto: **API del profesional para ver estado, listar documentos, subir/reemplazar documentos, ver historial y enviar a revisión**. El repomix confirma que ya existen los modelos `ProfessionalDocument`, `ProfessionalVerificationSubmission`, `ProfessionalVerificationEvent`, los bloqueantes en `ProfessionalProfile.get_submission_blockers()` y la lógica de envío en `services.py`, pero todavía no aparece una API equivalente a la de `appointments` para este flujo.  

Riesgos controlados en este cambio:

* no se cambian tablas ni migraciones;
* no se toca frontend;
* no se toca el flujo admin todavía;
* se evita duplicidad de documentos obligatorios reutilizando el mismo registro por tipo;
* no se borran archivos físicos anteriores para no perder evidencia; eso puede dejar archivos huérfanos, pero evita pérdida de datos.

Lo que sigue faltando después de esta fase será el **API admin de revisión**. Esta fase deja listo el lado del profesional, no todo el circuito completo. El propio repositorio indica que el siguiente paso lógico era REST API o pulido del admin. 

#### Preparación

Antes de aplicar:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py showmigrations
```

Backup de base:

```bash
docker exec -t ubemedicos_postgres pg_dump -U appuser -d appdb > backup_pre_professional_verification_api.sql
```

No hay cambio de base de datos en esta fase, así que no se necesita migración nueva.

---

#### Fase 1

**Objetivo**

Exponer por API el flujo del profesional para:

* ver su estado de verificación;
* listar documentos;
* subir o reemplazar documentos;
* ver historial;
* enviar expediente a revisión.

**Riesgo controlado**

No inventar reglas nuevas. La API se apoya en lo que ya existe en `models.py` y `services.py`: documentos por tipo, submissions abiertas únicas y envío con bloqueantes.  

**Archivo a tocar**

`backend/apps/professionals/api_views.py`
Acción: **agregar archivo nuevo**

**Código exacto**

```python
# INICIO CAMBIO NUEVO
import os
import uuid
from pathlib import Path

from django.core.files.uploadedfile import UploadedFile
from django.core.exceptions import ValidationError
from django.http import JsonResponse
from django.utils import timezone
from django.views.decorators.http import require_http_methods

from apps.users.api_auth import api_roles_required
from .models import (
    ProfessionalProfile,
    ProfessionalDocument,
    ProfessionalVerificationSubmission,
    ProfessionalVerificationEvent,
)
from .services import submit_professional_for_verification


BASE_BACKEND_DIR = Path(__file__).resolve().parents[2]
PROFESSIONAL_UPLOADS_DIR = BASE_BACKEND_DIR / "media" / "professional_documents"


def _json_error(message, status=400, extra=None):
    payload = {"detail": message}
    if extra:
        payload.update(extra)
    return JsonResponse(payload, status=status)


def _get_professional_profile(user):
    try:
        return user.professional_profile
    except ProfessionalProfile.DoesNotExist:
        return None


def _serialize_document(doc: ProfessionalDocument):
    return {
        "id": doc.id,
        "document_type": doc.document_type,
        "file_path": doc.file_path,
        "original_name": doc.original_name,
        "review_status": doc.review_status,
        "reviewer_notes": doc.reviewer_notes,
        "uploaded_at": doc.uploaded_at.isoformat() if doc.uploaded_at else None,
        "reviewed_at": doc.reviewed_at.isoformat() if doc.reviewed_at else None,
    }


def _serialize_submission(submission: ProfessionalVerificationSubmission):
    return {
        "id": submission.id,
        "status": submission.status,
        "submitted_at": submission.submitted_at.isoformat()
        if submission.submitted_at
        else None,
        "assigned_admin_id": submission.assigned_admin_id,
        "reviewed_by_id": submission.reviewed_by_id,
        "reviewed_at": submission.reviewed_at.isoformat()
        if submission.reviewed_at
        else None,
        "reviewer_notes": submission.reviewer_notes,
    }


def _serialize_event(event: ProfessionalVerificationEvent):
    return {
        "id": event.id,
        "event_type": event.event_type,
        "event_payload": event.event_payload,
        "created_by_id": event.created_by_id,
        "created_at": event.created_at.isoformat() if event.created_at else None,
    }


def _allowed_document_types():
    return {choice[0] for choice in ProfessionalDocument.DocumentType.choices}


def _write_uploaded_file(professional_id: int, uploaded_file: UploadedFile) -> str:
    suffix = Path(uploaded_file.name).suffix or ".bin"
    target_dir = PROFESSIONAL_UPLOADS_DIR / str(professional_id)
    target_dir.mkdir(parents=True, exist_ok=True)

    filename = f"{timezone.now().strftime('%Y%m%d%H%M%S')}_{uuid.uuid4().hex}{suffix}"
    full_path = target_dir / filename

    with open(full_path, "wb+") as destination:
        for chunk in uploaded_file.chunks():
            destination.write(chunk)

    return str(full_path)


@require_http_methods(["GET"])
@api_roles_required("professional")
def professional_verification_status_view(request):
    professional = _get_professional_profile(request.api_user)
    if not professional:
        return _json_error("El usuario no tiene perfil profesional.", status=404)

    latest_submission = professional.verification_submissions.order_by("-submitted_at").first()

    return JsonResponse(
        {
            "profile": {
                "professional_id": professional.id,
                "verification_status": professional.verification_status,
                "public_profile_enabled": professional.public_profile_enabled,
                "can_submit_for_verification": professional.can_submit_for_verification(),
                "submission_blockers": professional.get_submission_blockers(),
                "specialty_id": professional.specialty_id,
                "license_number": professional.license_number,
                "city": professional.city,
                "province": professional.province,
            },
            "latest_submission": _serialize_submission(latest_submission)
            if latest_submission
            else None,
        },
        status=200,
    )


@require_http_methods(["GET", "POST"])
@api_roles_required("professional")
def professional_documents_collection_view(request):
    professional = _get_professional_profile(request.api_user)
    if not professional:
        return _json_error("El usuario no tiene perfil profesional.", status=404)

    if request.method == "GET":
        documents = professional.documents.order_by("-uploaded_at", "-id")
        return JsonResponse(
            {
                "items": [_serialize_document(doc) for doc in documents],
            },
            status=200,
        )

    document_type = (request.POST.get("document_type") or "").strip()
    uploaded_file = request.FILES.get("file")

    if document_type not in _allowed_document_types():
        return _json_error(
            "document_type inválido.",
            status=400,
            extra={"allowed_document_types": sorted(_allowed_document_types())},
        )

    if uploaded_file is None:
        return _json_error("file es obligatorio.", status=400)

    saved_path = _write_uploaded_file(professional.id, uploaded_file)

    # Regla conservadora:
    # - Para documentos obligatorios y SENESCYT se reutiliza un solo registro por tipo.
    # - Para OTHER se permite crear varios registros.
    if document_type == ProfessionalDocument.DocumentType.OTHER:
        document = ProfessionalDocument.objects.create(
            professional=professional,
            document_type=document_type,
            file_path=saved_path,
            original_name=uploaded_file.name,
            review_status=ProfessionalDocument.ReviewStatus.PENDING,
            reviewer_notes="",
            reviewed_at=None,
        )
    else:
        document, _ = ProfessionalDocument.objects.update_or_create(
            professional=professional,
            document_type=document_type,
            defaults={
                "file_path": saved_path,
                "original_name": uploaded_file.name,
                "review_status": ProfessionalDocument.ReviewStatus.PENDING,
                "reviewer_notes": "",
                "reviewed_at": None,
            },
        )

    return JsonResponse(_serialize_document(document), status=201)


@require_http_methods(["PATCH"])
@api_roles_required("professional")
def professional_document_replace_view(request, document_id: int):
    professional = _get_professional_profile(request.api_user)
    if not professional:
        return _json_error("El usuario no tiene perfil profesional.", status=404)

    try:
        document = ProfessionalDocument.objects.get(
            id=document_id,
            professional=professional,
        )
    except ProfessionalDocument.DoesNotExist:
        return _json_error("Documento no encontrado.", status=404)

    uploaded_file = request.FILES.get("file")
    if uploaded_file is None:
        return _json_error("file es obligatorio.", status=400)

    saved_path = _write_uploaded_file(professional.id, uploaded_file)

    document.file_path = saved_path
    document.original_name = uploaded_file.name
    document.review_status = ProfessionalDocument.ReviewStatus.PENDING
    document.reviewer_notes = ""
    document.reviewed_at = None
    document.save(
        update_fields=[
            "file_path",
            "original_name",
            "review_status",
            "reviewer_notes",
            "reviewed_at",
        ]
    )

    return JsonResponse(_serialize_document(document), status=200)


@require_http_methods(["POST"])
@api_roles_required("professional")
def professional_verification_submit_view(request):
    professional = _get_professional_profile(request.api_user)
    if not professional:
        return _json_error("El usuario no tiene perfil profesional.", status=404)

    try:
        submission = submit_professional_for_verification(
            professional=professional,
            actor=request.api_user,
        )
    except ValidationError as exc:
        messages = list(exc.messages) if hasattr(exc, "messages") else [str(exc)]
        return JsonResponse(
            {
                "detail": "No se pudo enviar a revisión.",
                "errors": messages,
                "submission_blockers": professional.get_submission_blockers(),
            },
            status=400,
        )

    return JsonResponse(
        {
            "detail": "Expediente enviado a revisión correctamente.",
            "submission": _serialize_submission(submission),
        },
        status=201,
    )


@require_http_methods(["GET"])
@api_roles_required("professional")
def professional_verification_events_view(request):
    professional = _get_professional_profile(request.api_user)
    if not professional:
        return _json_error("El usuario no tiene perfil profesional.", status=404)

    events = ProfessionalVerificationEvent.objects.filter(
        submission__professional=professional
    ).order_by("-created_at", "-id")

    return JsonResponse(
        {
            "items": [_serialize_event(event) for event in events],
        },
        status=200,
    )
# FIN CAMBIO NUEVO
```

**Archivo a tocar**

`backend/apps/professionals/api_urls.py`
Acción: **agregar archivo nuevo**

**Código exacto**

```python
# INICIO CAMBIO NUEVO
from django.urls import path

from .api_views import (
    professional_verification_status_view,
    professional_documents_collection_view,
    professional_document_replace_view,
    professional_verification_submit_view,
    professional_verification_events_view,
)

urlpatterns = [
    path(
        "professional/verification/status",
        professional_verification_status_view,
        name="professional-verification-status",
    ),
    path(
        "professional/documents",
        professional_documents_collection_view,
        name="professional-documents-collection",
    ),
    path(
        "professional/documents/<int:document_id>",
        professional_document_replace_view,
        name="professional-document-replace",
    ),
    path(
        "professional/verification/submit",
        professional_verification_submit_view,
        name="professional-verification-submit",
    ),
    path(
        "professional/verification/events",
        professional_verification_events_view,
        name="professional-verification-events",
    ),
]
# FIN CAMBIO NUEVO
```

**Archivo a tocar**

`backend/config/urls.py`
Acción: **reemplazar archivo completo**

El repomix muestra que hoy están montadas las rutas de `users`, y en el árbol más reciente ya aparece también `appointments`. Esta fase agrega `professionals` junto a esas rutas.  

**Código exacto**

```python
# INICIO CAMBIO NUEVO
from django.contrib import admin
from django.urls import include, path

urlpatterns = [
    path("admin/", admin.site.urls),
    path("api/v1/", include("apps.users.api_urls")),
    path("api/v1/", include("apps.appointments.api_urls")),
    path("api/v1/", include("apps.professionals.api_urls")),
]
# FIN CAMBIO NUEVO
```

**Cómo probar**

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
```

**Qué debe seguir funcionando**

* login y auth por token/cookie;
* rutas actuales de `users`;
* disponibilidad profesional;
* `ProfessionalProfile.get_submission_blockers()`;
* `submit_professional_for_verification()`.

Todo eso ya existe y esta fase se apoya en ello.   

**Cómo revertir esa fase**

1. Quitar `include("apps.professionals.api_urls")` de `backend/config/urls.py`
2. Eliminar:

   * `backend/apps/professionals/api_views.py`
   * `backend/apps/professionals/api_urls.py`

---

#### Prueba de Fase 1

**Archivo a tocar**

`backend/apps/professionals/tests_api.py`
Acción: **agregar archivo nuevo**

**Código exacto**

```python
# INICIO CAMBIO NUEVO
from django.contrib.auth import get_user_model
from django.core.files.uploadedfile import SimpleUploadedFile
from django.test import TestCase

from apps.catalogs.models import Specialty
from .models import (
    ProfessionalProfile,
    ProfessionalDocument,
    ProfessionalVerificationSubmission,
)


class ProfessionalVerificationApiTests(TestCase):
    def setUp(self):
        User = get_user_model()

        self.prof_user = User.objects.create_user(
            email="proapi@test.local",
            password="Pro123456!",
            first_name="Ana",
            last_name="Ruiz",
            role="professional",
            is_email_verified=True,
        )

        self.admin_user = User.objects.create_user(
            email="adminapi@test.local",
            password="Admin123!",
            first_name="Admin",
            last_name="Test",
            role="admin",
            is_staff=True,
        )

        self.specialty = Specialty.objects.create(name="Medicina General")

        self.profile = ProfessionalProfile.objects.create(
            user=self.prof_user,
            specialty=self.specialty,
            license_number="LIC-API-001",
            city="Quito",
            province="Pichincha",
        )

        login = self.client.post(
            "/api/v1/auth/login",
            data='{"email":"proapi@test.local","password":"Pro123456!"}',
            content_type="application/json",
        )
        self.token = login.json()["access_token"]

    def _auth_headers(self):
        return {"HTTP_AUTHORIZATION": f"Bearer {self.token}"}

    def _pdf_file(self, name="doc.pdf", content=b"%PDF-1.4 test file"):
        return SimpleUploadedFile(name, content, content_type="application/pdf")

    def test_get_verification_status(self):
        response = self.client.get(
            "/api/v1/professional/verification/status",
            **self._auth_headers(),
        )
        self.assertEqual(response.status_code, 200)
        self.assertIn("profile", response.json())
        self.assertEqual(
            response.json()["profile"]["verification_status"],
            ProfessionalProfile.VerificationStatus.DRAFT,
        )

    def test_upload_document(self):
        response = self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.ID,
                "file": self._pdf_file("id.pdf"),
            },
            **self._auth_headers(),
        )
        self.assertEqual(response.status_code, 201)
        self.assertTrue(
            ProfessionalDocument.objects.filter(
                professional=self.profile,
                document_type=ProfessionalDocument.DocumentType.ID,
            ).exists()
        )

    def test_upload_same_required_document_reuses_record(self):
        first = self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.TITLE,
                "file": self._pdf_file("title_v1.pdf"),
            },
            **self._auth_headers(),
        )
        self.assertEqual(first.status_code, 201)

        second = self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.TITLE,
                "file": self._pdf_file("title_v2.pdf"),
            },
            **self._auth_headers(),
        )
        self.assertEqual(second.status_code, 201)

        self.assertEqual(
            ProfessionalDocument.objects.filter(
                professional=self.profile,
                document_type=ProfessionalDocument.DocumentType.TITLE,
            ).count(),
            1,
        )

    def test_submit_verification_success(self):
        self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.ID,
                "file": self._pdf_file("id.pdf"),
            },
            **self._auth_headers(),
        )
        self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.TITLE,
                "file": self._pdf_file("title.pdf"),
            },
            **self._auth_headers(),
        )
        self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.LICENSE,
                "file": self._pdf_file("license.pdf"),
            },
            **self._auth_headers(),
        )

        response = self.client.post(
            "/api/v1/professional/verification/submit",
            **self._auth_headers(),
        )

        self.assertEqual(response.status_code, 201)
        self.profile.refresh_from_db()
        self.assertEqual(
            self.profile.verification_status,
            ProfessionalProfile.VerificationStatus.SUBMITTED,
        )
        self.assertEqual(
            ProfessionalVerificationSubmission.objects.filter(
                professional=self.profile,
                status=ProfessionalVerificationSubmission.Status.SUBMITTED,
            ).count(),
            1,
        )

    def test_submit_verification_returns_blockers(self):
        response = self.client.post(
            "/api/v1/professional/verification/submit",
            **self._auth_headers(),
        )
        self.assertEqual(response.status_code, 400)
        self.assertIn("submission_blockers", response.json())
        self.assertGreater(len(response.json()["submission_blockers"]), 0)

    def test_list_verification_events(self):
        self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.ID,
                "file": self._pdf_file("id.pdf"),
            },
            **self._auth_headers(),
        )
        self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.TITLE,
                "file": self._pdf_file("title.pdf"),
            },
            **self._auth_headers(),
        )
        self.client.post(
            "/api/v1/professional/documents",
            data={
                "document_type": ProfessionalDocument.DocumentType.LICENSE,
                "file": self._pdf_file("license.pdf"),
            },
            **self._auth_headers(),
        )
        self.client.post(
            "/api/v1/professional/verification/submit",
            **self._auth_headers(),
        )

        response = self.client.get(
            "/api/v1/professional/verification/events",
            **self._auth_headers(),
        )

        self.assertEqual(response.status_code, 200)
        self.assertIn("items", response.json())
        self.assertGreaterEqual(len(response.json()["items"]), 1)
# FIN CAMBIO NUEVO
```

**Cómo probar**

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py test apps.professionals.tests apps.professionals.tests_api apps.users.tests_api -v 2
```

**Qué debe seguir funcionando**

* tests actuales de `professionals` que validan el flujo de servicio;
* login API;
* disponibilidad API.

El proyecto ya trae tests del flujo de servicio para aprobación, rechazo y corrección, por eso esta fase agrega solo tests API encima de esa base. 

**Cómo revertir esa fase**

Eliminar:

```text
backend/apps/professionals/tests_api.py
```

---

#### Fase 2

**Objetivo**

Probar manualmente el flujo real del profesional sin hacer frontend todavía.

**Riesgo controlado**

Se valida backend real antes de abrir pantallas.

**Archivo a tocar**

Ninguno adicional.

**Cómo probar**

1. Login:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"profesional@demo.local\",\"password\":\"Profesional123!\"}"
```

2. Ver estado:

```bash
curl http://127.0.0.1:8000/api/v1/professional/verification/status ^
  -H "Authorization: Bearer TOKEN_AQUI"
```

3. Subir documento ID:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/professional/documents ^
  -H "Authorization: Bearer TOKEN_AQUI" ^
  -F "document_type=id" ^
  -F "file=@C:\ruta\id.pdf"
```

4. Subir título:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/professional/documents ^
  -H "Authorization: Bearer TOKEN_AQUI" ^
  -F "document_type=title" ^
  -F "file=@C:\ruta\title.pdf"
```

5. Subir licencia o SENESCYT:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/professional/documents ^
  -H "Authorization: Bearer TOKEN_AQUI" ^
  -F "document_type=license" ^
  -F "file=@C:\ruta\license.pdf"
```

6. Enviar a revisión:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/professional/verification/submit ^
  -H "Authorization: Bearer TOKEN_AQUI"
```

7. Ver historial:

```bash
curl http://127.0.0.1:8000/api/v1/professional/verification/events ^
  -H "Authorization: Bearer TOKEN_AQUI"
```

**Qué debe seguir funcionando**

* el profesional debe quedar en `submitted` al enviar, porque así lo define el servicio actual;
* solo debe permitirse desde `draft`, `rejected` o `needs_correction`;
* debe bloquearse si faltan documentos obligatorios o si ya existe una submission abierta. 

**Cómo revertir esa fase**

No aplica; son pruebas manuales.

---

#### Pruebas de regresión

Ejecutar:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.professionals.tests -v 2
python manage.py test apps.professionals.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
```

Validaciones manuales mínimas:

* un profesional sin documentos recibe blockers;
* al subir `id`, `title` y `license` o `senescyt`, puede enviar;
* al reenviar un documento obligatorio, el estado del documento vuelve a `pending`;
* se crea al menos un evento en historial después del submit.

---

#### Plan de reversión total

1. Restaurar `backend/config/urls.py` sin `apps.professionals.api_urls`

2. Eliminar:

   * `backend/apps/professionals/api_views.py`
   * `backend/apps/professionals/api_urls.py`
   * `backend/apps/professionals/tests_api.py`

3. Ejecutar:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py test apps.professionals.tests apps.users.tests_api -v 2
```

4. Si algo mayor fallara, restaurar backup de base:

```bash
docker exec -i ubemedicos_postgres psql -U appuser -d appdb < backup_pre_professional_verification_api.sql
```

Lo siguiente que tocaría, manteniendo el orden correcto antes de pantallas web, es dejar **la Fase 2 en el mismo formato copiar y pegar**: el **API admin para revisar expediente, aprobar, rechazar, pedir corrección y revisar documentos**.



---


#### Impacto y riesgos

Ya quedó preparado el siguiente bloque real de la secuencia: exponer por API el expediente documental del profesional. El repomix muestra que `config/urls.py` ya monta `users` y `appointments`, pero todavía no publica `professionals`, mientras que la lógica de validación profesional ya existe en modelos, servicios y pruebas.   

Riesgos controlados en este bloque:

* no se toca base de datos;
* no se agregan migraciones;
* no se toca frontend;
* no se modifica el flujo admin existente;
* el reemplazo de PDF no elimina el archivo anterior, para no arriesgar evidencia.

#### Preparación

Paquete listo para copiar y pegar:

* [ZIP del bloque API profesional](sandbox:/mnt/data/professional_verification_api_patch.zip)
* [Carpeta con archivos preparados](sandbox:/mnt/data/professional_verification_api_patch)
* [Patch unificado](sandbox:/mnt/data/professional_verification_api_patch/professional_verification_api.patch)

Validación previa recomendada:

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests -v 2
```

#### Fase 1

**Objetivo**

Agregar los endpoints faltantes del profesional:

* `GET/PATCH /api/v1/professional/profile`
* `GET/POST /api/v1/professional/documents`
* `GET /api/v1/professional/verification/status`
* `POST /api/v1/professional/verification/submit`
* `GET /api/v1/professional/verification/events`

**Riesgo controlado**

Se cierra backend antes de pasar a más pantallas web.

**Archivo a tocar**

* `backend/apps/professionals/api_views.py` → archivo nuevo
* `backend/apps/professionals/api_urls.py` → archivo nuevo
* `backend/config/urls.py` → reemplazo completo

**Código exacto**

Ya quedó dentro del paquete.

Archivos directos:

* [api_views.py](sandbox:/mnt/data/professional_verification_api_patch/backend/apps/professionals/api_views.py)
* [api_urls.py](sandbox:/mnt/data/professional_verification_api_patch/backend/apps/professionals/api_urls.py)
* [config/urls.py](sandbox:/mnt/data/professional_verification_api_patch/backend/config/urls.py)

**Cómo probar**

```bash
cd backend
python manage.py check
```

**Qué debe seguir funcionando**

* `/api/v1/auth/login`
* `/api/v1/auth/me`
* `/api/v1/users/me`
* `/api/v1/professional/availability`

**Cómo revertir esa fase**

Quitar esta línea de `backend/config/urls.py`:

```python
path("api/v1/", include("apps.professionals.api_urls")),
```

Y eliminar:

* `backend/apps/professionals/api_views.py`
* `backend/apps/professionals/api_urls.py`

#### Prueba de Fase 1

Pruebas manuales mínimas:

1. login del profesional;
2. consultar perfil;
3. actualizar ciudad, provincia y licencia;
4. subir PDF;
5. consultar estado;
6. enviar expediente;
7. consultar eventos.

Ejemplo de ejecución:

```bash
curl http://127.0.0.1:8000/api/v1/professional/profile ^
  -H "Authorization: Bearer TOKEN_AQUI"
```

#### Fase 2

**Objetivo**

Agregar pruebas automáticas para que el programador junior valide el bloque sin depender del frontend.

**Riesgo controlado**

Se detectan regresiones en submit, reemplazo de documentos y listado de eventos.

**Archivo a tocar**

* `backend/apps/professionals/tests_api.py` → archivo nuevo

**Código exacto**

* [tests_api.py](sandbox:/mnt/data/professional_verification_api_patch/backend/apps/professionals/tests_api.py)

**Cómo probar**

```bash
cd backend
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests apps.professionals.tests_api -v 2
```

**Qué debe seguir funcionando**

* tests actuales de `users`
* tests actuales de `appointments`
* tests actuales de `professionals`

**Cómo revertir esa fase**

Eliminar:

* `backend/apps/professionals/tests_api.py`

#### Prueba de Fase 2

Se debe validar que:

* un `document_type` repetido no duplique filas;
* el submit falle con blockers si faltan PDFs;
* el submit exitoso cambie el estado a `submitted`;
* el historial de eventos devuelva al menos un item.

#### Pruebas de regresión

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.professionals.tests -v 2
python manage.py test apps.professionals.tests_api -v 2
```

#### Plan de reversión total

1. Restaurar `backend/config/urls.py` al estado anterior.
2. Eliminar:

   * `backend/apps/professionals/api_views.py`
   * `backend/apps/professionals/api_urls.py`
   * `backend/apps/professionals/tests_api.py`
3. Ejecutar:

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests -v 2
```

El siguiente bloque, después de estabilizar esto, sería la API administrativa de revisión: listar pendientes, asignar, pasar a `under_review`, aprobar, rechazar y pedir corrección.



---
#### Impacto y riesgos

Ya quedó preparado el siguiente bloque real de la secuencia: exponer por API el expediente documental del profesional. El repomix muestra que `config/urls.py` ya monta `users` y `appointments`, pero todavía no publica `professionals`, mientras que la lógica de validación profesional ya existe en modelos, servicios y pruebas.   

Riesgos controlados en este bloque:

* no se toca base de datos;
* no se agregan migraciones;
* no se toca frontend;
* no se modifica el flujo admin existente;
* el reemplazo de PDF no elimina el archivo anterior, para no arriesgar evidencia.

#### Preparación

Paquete listo para copiar y pegar:

* [ZIP del bloque API profesional](sandbox:/mnt/data/professional_verification_api_patch.zip)
* [Carpeta con archivos preparados](sandbox:/mnt/data/professional_verification_api_patch)
* [Patch unificado](sandbox:/mnt/data/professional_verification_api_patch/professional_verification_api.patch)

Validación previa recomendada:

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests -v 2
```

#### Fase 1

**Objetivo**

Agregar los endpoints faltantes del profesional:

* `GET/PATCH /api/v1/professional/profile`
* `GET/POST /api/v1/professional/documents`
* `GET /api/v1/professional/verification/status`
* `POST /api/v1/professional/verification/submit`
* `GET /api/v1/professional/verification/events`

**Riesgo controlado**

Se cierra backend antes de pasar a más pantallas web.

**Archivo a tocar**

* `backend/apps/professionals/api_views.py` → archivo nuevo
* `backend/apps/professionals/api_urls.py` → archivo nuevo
* `backend/config/urls.py` → reemplazo completo

**Código exacto**

Ya quedó dentro del paquete.

Archivos directos:

* [api_views.py](sandbox:/mnt/data/professional_verification_api_patch/backend/apps/professionals/api_views.py)
* [api_urls.py](sandbox:/mnt/data/professional_verification_api_patch/backend/apps/professionals/api_urls.py)
* [config/urls.py](sandbox:/mnt/data/professional_verification_api_patch/backend/config/urls.py)

**Cómo probar**

```bash
cd backend
python manage.py check
```

**Qué debe seguir funcionando**

* `/api/v1/auth/login`
* `/api/v1/auth/me`
* `/api/v1/users/me`
* `/api/v1/professional/availability`

**Cómo revertir esa fase**

Quitar esta línea de `backend/config/urls.py`:

```python
path("api/v1/", include("apps.professionals.api_urls")),
```

Y eliminar:

* `backend/apps/professionals/api_views.py`
* `backend/apps/professionals/api_urls.py`

#### Prueba de Fase 1

Pruebas manuales mínimas:

1. login del profesional;
2. consultar perfil;
3. actualizar ciudad, provincia y licencia;
4. subir PDF;
5. consultar estado;
6. enviar expediente;
7. consultar eventos.

Ejemplo de ejecución:

```bash
curl http://127.0.0.1:8000/api/v1/professional/profile ^
  -H "Authorization: Bearer TOKEN_AQUI"
```

#### Fase 2

**Objetivo**

Agregar pruebas automáticas para que el programador junior valide el bloque sin depender del frontend.

**Riesgo controlado**

Se detectan regresiones en submit, reemplazo de documentos y listado de eventos.

**Archivo a tocar**

* `backend/apps/professionals/tests_api.py` → archivo nuevo

**Código exacto**

* [tests_api.py](sandbox:/mnt/data/professional_verification_api_patch/backend/apps/professionals/tests_api.py)

**Cómo probar**

```bash
cd backend
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests apps.professionals.tests_api -v 2
```

**Qué debe seguir funcionando**

* tests actuales de `users`
* tests actuales de `appointments`
* tests actuales de `professionals`

**Cómo revertir esa fase**

Eliminar:

* `backend/apps/professionals/tests_api.py`

#### Prueba de Fase 2

Se debe validar que:

* un `document_type` repetido no duplique filas;
* el submit falle con blockers si faltan PDFs;
* el submit exitoso cambie el estado a `submitted`;
* el historial de eventos devuelva al menos un item.

#### Pruebas de regresión

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.professionals.tests -v 2
python manage.py test apps.professionals.tests_api -v 2
```

#### Plan de reversión total

1. Restaurar `backend/config/urls.py` al estado anterior.
2. Eliminar:

   * `backend/apps/professionals/api_views.py`
   * `backend/apps/professionals/api_urls.py`
   * `backend/apps/professionals/tests_api.py`
3. Ejecutar:

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests -v 2
```

El siguiente bloque, después de estabilizar esto, sería la API administrativa de revisión: listar pendientes, asignar, pasar a `under_review`, aprobar, rechazar y pedir corrección.


----


#### Impacto y riesgos

El siguiente bloque lógico ya quedó armado: la **API administrativa de revisión documental**. El repomix muestra que la lógica crítica ya existe en `apps.professionals.services.py` para asignar solicitudes, iniciar revisión, revisar documentos, pedir corrección, aprobar y rechazar; además, la propia documentación interna del proyecto marca como siguiente paso natural exponer API y pulir administración. 

Este bloque no toca base de datos, no agrega migraciones y no cambia frontend. El riesgo principal está en publicar endpoints sin respetar los estados reales del flujo, por eso todo quedó montado sobre los servicios ya existentes, sin duplicar reglas de negocio. También se limita el acceso a `admin` y `super_admin`, para no abrir acciones de revisión a otros roles.

Paquete listo para copiar y pegar:

* [ZIP del bloque admin review API](sandbox:/mnt/data/admin_verification_api_patch.zip)
* [Carpeta con archivos preparados](sandbox:/mnt/data/admin_verification_api_patch)
* [Patch unificado](sandbox:/mnt/data/admin_verification_api_patch/admin_verification_api.patch)

#### Preparación

Supuesto conservador: primero debe estar aplicado el bloque anterior del expediente del profesional por API, porque este nuevo paso reemplaza `backend/apps/professionals/api_views.py` y `backend/apps/professionals/api_urls.py` partiendo de esa versión. El repositorio original del repomix todavía no trae esos archivos, y `config/urls.py` hoy solo incluye `users` y `appointments`, así que este bloque se entrega como continuación directa del anterior.

Antes de aplicar:

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests apps.professionals.tests_api -v 2
```

#### Fase 1

**Objetivo**

Publicar la cola administrativa y el detalle del expediente.

**Riesgo controlado**

No se inventan estados ni validaciones nuevas. Solo se exponen lectura y detalle sobre modelos y servicios ya existentes.

**Archivos a tocar**

* `backend/apps/professionals/api_views.py` → reemplazo completo
* `backend/apps/professionals/api_urls.py` → reemplazo completo
* `backend/config/urls.py` → reemplazo completo

**Qué agrega esta fase**

Rutas nuevas:

* `GET /api/v1/admin/professional-verifications`
* `GET /api/v1/admin/professional-verifications/<submission_id>`

La cola permite filtrar por:

* `status`
* `professional_email`
* `assigned_admin_id`
* `mine=1`

**Código exacto**

Archivos listos:

* [api_views.py](sandbox:/mnt/data/admin_verification_api_patch/backend/apps/professionals/api_views.py)
* [api_urls.py](sandbox:/mnt/data/admin_verification_api_patch/backend/apps/professionals/api_urls.py)
* [config/urls.py](sandbox:/mnt/data/admin_verification_api_patch/backend/config/urls.py)

**Cómo probar**

```bash
cd backend
python manage.py check
```

**Qué debe seguir funcionando**

* `/api/v1/auth/login`
* `/api/v1/auth/me`
* `/api/v1/professional/profile`
* `/api/v1/professional/documents`
* `/api/v1/professional/verification/status`
* `/api/v1/professional/verification/submit`
* `/api/v1/professional/verification/events`

**Cómo revertir esta fase**

Restaurar los archivos previos de:

* `backend/apps/professionals/api_views.py`
* `backend/apps/professionals/api_urls.py`
* `backend/config/urls.py`

#### Prueba de Fase 1

Pruebas manuales mínimas:

1. login con usuario admin;
2. consultar cola:

```bash
curl http://127.0.0.1:8000/api/v1/admin/professional-verifications \
  -H "Authorization: Bearer TOKEN_ADMIN"
```

3. consultar detalle:

```bash
curl http://127.0.0.1:8000/api/v1/admin/professional-verifications/1 \
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Se espera:

* `200 OK`
* lista de submissions abiertas por defecto;
* detalle con submission, perfil, documentos y eventos.

#### Fase 2

**Objetivo**

Publicar las acciones administrativas de revisión.

**Riesgo controlado**

Cada acción llama al servicio correspondiente, así que el estado no se altera por fuera del flujo oficial.

**Archivos a tocar**

* `backend/apps/professionals/api_views.py` → ya incluido en el reemplazo completo
* `backend/apps/professionals/api_urls.py` → ya incluido en el reemplazo completo

**Qué agrega esta fase**

Rutas nuevas:

* `POST /api/v1/admin/professional-verifications/<submission_id>/assign`
* `POST /api/v1/admin/professional-verifications/<submission_id>/start-review`
* `POST /api/v1/admin/professional-verifications/<submission_id>/documents/<document_id>/review`
* `POST /api/v1/admin/professional-verifications/<submission_id>/request-correction`
* `POST /api/v1/admin/professional-verifications/<submission_id>/approve`
* `POST /api/v1/admin/professional-verifications/<submission_id>/reject`

**Comportamiento**

* `assign` asigna a otro admin o al admin actual si no se envía `assignee_user_id`.
* `start-review` pasa de `submitted` a `under_review`.
* `review document` acepta `approved` o `rejected`.
* `request-correction` exige observaciones.
* `approve` solo funciona si identidad, título y licencia/SENESCYT están aprobados.
* `reject` exige observaciones.

**Cómo probar**

Ejemplos:

Asignar:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/admin/professional-verifications/1/assign \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_ADMIN" \
  -d '{"assignee_user_id": 2}'
```

Pasar a revisión:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/admin/professional-verifications/1/start-review \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_ADMIN" \
  -d '{}'
```

Revisar documento:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/admin/professional-verifications/1/documents/10/review \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_ADMIN" \
  -d '{"decision":"approved","notes":"Documento correcto"}'
```

Pedir corrección:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/admin/professional-verifications/1/request-correction \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_ADMIN" \
  -d '{"notes":"Debe reemplazar la licencia"}'
```

Aprobar:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/admin/professional-verifications/1/approve \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_ADMIN" \
  -d '{"notes":"Aprobado por revisión administrativa"}'
```

Rechazar:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/admin/professional-verifications/1/reject \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN_ADMIN" \
  -d '{"notes":"La documentación no coincide"}'
```

**Qué debe seguir funcionando**

* flujo del profesional ya entregado;
* servicios de validación ya existentes;
* admin Django tradicional.

**Cómo revertir esta fase**

Restaurar `api_views.py` y `api_urls.py` al estado previo del bloque anterior.

#### Prueba de Fase 2

Archivo nuevo de pruebas:

* [tests_api_admin.py](sandbox:/mnt/data/admin_verification_api_patch/backend/apps/professionals/tests_api_admin.py)

Casos cubiertos:

* listar submissions;
* ver detalle;
* asignar a otro admin;
* iniciar revisión;
* revisar documento;
* pedir corrección con y sin observaciones;
* aprobar cuando todos los documentos están aprobados;
* rechazar con y sin observaciones;
* confirmar que un profesional no puede entrar a la cola admin.

Ejecución:

```bash
cd backend
python manage.py test apps.professionals.tests_api_admin -v 2
```

#### Pruebas de regresión

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.professionals.tests -v 2
python manage.py test apps.professionals.tests_api -v 2
python manage.py test apps.professionals.tests_api_admin -v 2
```

También debe seguir funcionando:

* login de admin;
* login de profesional;
* envío de expediente por profesional;
* consulta de estado del expediente.

#### Plan de reversión total

1. Restaurar:

   * `backend/apps/professionals/api_views.py`
   * `backend/apps/professionals/api_urls.py`
   * `backend/config/urls.py`

2. Eliminar:

   * `backend/apps/professionals/tests_api_admin.py`

3. Ejecutar:

```bash
cd backend
python manage.py check
python manage.py test apps.users.tests_api apps.appointments.tests_api apps.professionals.tests apps.professionals.tests_api -v 2
```

El siguiente bloque de la secuencia, después de estabilizar esto, sería recién el **frontend admin de revisión documental** conectado a estas rutas.



----
