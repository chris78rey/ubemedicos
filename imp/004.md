Se implementa el siguiente paso visible para el paciente: **mostrar una leyenda precisa cuando la cita haya sido resuelta administrativamente con reembolso**. En el snapshot actual, la pantalla del paciente ya cruza citas con pagos por `appointment.id`, pero solo muestra un mensaje genérico para citas pagadas; además, el modelo de pagos sí guarda `raw_response`, mientras el endpoint de paciente solo devuelve la serialización base sin exponer un resumen seguro de esa resolución.   

## ⚠️ Análisis de Impacto y Riesgos

**Módulos afectados:**

* `backend/apps/payments/api_views.py`
* `backend/apps/payments/tests_api.py`
* `web_frontend/pages/patient/appointments.vue`

**Riesgos detectados:**

* Si se expone `raw_response` completo al paciente, se filtra demasiada información interna.
* Si no se expone nada, el paciente sigue viendo solo una leyenda ambigua.
* Si la UI depende de campos no seguros o no normalizados, se vuelve frágil.

**Supuestos:**

* La fase anterior ya deja trazas en `payment.raw_response` cuando la administración resuelve el caso con reembolso.
* Lo correcto para paciente es exponer un **resumen filtrado**, no el JSON completo.
* No se requiere migración de base de datos.

---

## 🛠️ Preparación y Backups

```powershell id="gq1a7r"
Copy-Item "backend\apps\payments\api_views.py" "backend\apps\payments\api_views.py.bak_patient_resolution_legend"
Copy-Item "backend\apps\payments\tests_api.py" "backend\apps\payments\tests_api.py.bak_patient_resolution_legend"
Copy-Item "web_frontend\pages\patient\appointments.vue" "web_frontend\pages\patient\appointments.vue.bak_patient_resolution_legend"
```

```bash id="vqlxx4"
cd backend
..\.venv\Scripts\python.exe manage.py check
```

---

## 🚀 Implementación por Fases

### Fase 1: Exponer un resumen seguro de resolución para el paciente

**Objetivo:** agregar a la respuesta de pagos del paciente un `resolution_summary` filtrado.
**Ruta del Archivo:** `backend/apps/payments/api_views.py`
**Acción:** **Insertar** una función nueva y **reemplazar** `_serialize_payment`.

#### 1A. Insertar este bloque **debajo de** `_new_external_reference()`

```python id="3we2za"
# --- INICIO CAMBIO NUEVO ---
def _build_patient_resolution_summary(payment: Payment):
    raw = payment.raw_response or {}
    decision = str(raw.get("decision") or "").strip()
    cancel_as = str(raw.get("cancel_as") or "").strip().lower() or None
    refunded_at = raw.get("refunded_at")
    appointment_status_after = raw.get("appointment_status_after") or payment.appointment.status

    if payment.status != Payment.Status.REFUNDED:
        return None

    if decision == "refund_and_cancel_appointment":
        if cancel_as == "patient":
            message = (
                "La cita fue resuelta por administración, quedó cancelada "
                "como cancelación del paciente y el pago fue reembolsado."
            )
        elif cancel_as == "professional":
            message = (
                "La cita fue resuelta por administración, quedó cancelada "
                "como cancelación del profesional y el pago fue reembolsado."
            )
        else:
            message = (
                "La cita fue resuelta por administración y el pago fue reembolsado."
            )

        return {
            "kind": "refunded_and_cancelled",
            "message": message,
            "cancelled_as": cancel_as,
            "appointment_status_after": appointment_status_after,
            "refunded_at": refunded_at,
        }

    if decision == "refunded":
        return {
            "kind": "refund_only",
            "message": "El pago fue reembolsado administrativamente.",
            "cancelled_as": None,
            "appointment_status_after": appointment_status_after,
            "refunded_at": refunded_at,
        }

    return {
        "kind": "refunded",
        "message": "El pago figura como reembolsado.",
        "cancelled_as": None,
        "appointment_status_after": appointment_status_after,
        "refunded_at": refunded_at,
    }
# --- FIN CAMBIO NUEVO ---
```

#### 1B. Reemplazar la función `_serialize_payment` completa

```python id="6p39m2"
# --- INICIO CAMBIO NUEVO ---
def _serialize_payment(payment: Payment, include_raw=False):
    appointment = payment.appointment
    payload = {
        "id": payment.id,
        "appointment": {
            "id": appointment.id,
            "status": appointment.status,
            "is_paid": appointment.is_paid,
            "scheduled_at": appointment.scheduled_at.isoformat(),
            "ends_at": appointment.ends_at.isoformat(),
            "modality": appointment.modality,
            "price": str(appointment.price),
            "professional": {
                "id": appointment.professional_id,
                "name": f"{appointment.professional.user.first_name} {appointment.professional.user.last_name}".strip(),
                "specialty": appointment.professional.specialty.name if appointment.professional.specialty else None,
            },
            "patient": {
                "id": appointment.patient_id,
                "name": f"{appointment.patient.user.first_name} {appointment.patient.user.last_name}".strip(),
            },
        },
        "external_reference": payment.external_reference,
        "amount": str(payment.amount),
        "currency": payment.currency,
        "status": payment.status,
        "paid_at": payment.paid_at.isoformat() if payment.paid_at else None,
        "created_at": payment.created_at.isoformat(),
        "resolution_summary": _build_patient_resolution_summary(payment),
    }
    if include_raw:
        payload["raw_response"] = payment.raw_response
    return payload
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 1:**
Un pago `refunded` con `raw_response.decision = refund_and_cancel_appointment` debe devolver `resolution_summary.kind = refunded_and_cancelled`.

**Rollback Fase 1**

```powershell id="x7v1sr"
Copy-Item "backend\apps\payments\api_views.py.bak_patient_resolution_legend" "backend\apps\payments\api_views.py" -Force
```

---

### Fase 2: Blindar el backend con prueba automática

**Objetivo:** garantizar que el paciente reciba el resumen correcto.
**Ruta del Archivo:** `backend/apps/payments/tests_api.py`
**Acción:** **Agregar** este método al final de la clase `PaymentDemoApiTests`.

```python id="m9ndru"
# --- INICIO CAMBIO NUEVO ---
    def test_patient_list_payments_includes_resolution_summary_for_refunded_and_cancelled(self):
        payment = Payment.objects.create(
            appointment=self.appointment,
            external_reference="demo_reference_030",
            amount=self.appointment.price,
            currency="USD",
            status=Payment.Status.REFUNDED,
            paid_at=timezone.now(),
            raw_response={
                "decision": "refund_and_cancel_appointment",
                "cancel_as": "professional",
                "refunded_at": timezone.now().isoformat(),
                "appointment_status_after": Appointment.Status.CANCELLED_BY_PROFESSIONAL,
            },
        )

        self.appointment.status = Appointment.Status.CANCELLED_BY_PROFESSIONAL
        self.appointment.is_paid = False
        self.appointment.save(update_fields=["status", "is_paid"])

        response = self.client.get(
            "/api/v1/patient/payments",
            **self._auth_headers(self.patient_token),
        )
        self.assertEqual(response.status_code, 200)

        items = response.json()["items"]
        self.assertEqual(len(items), 1)
        self.assertEqual(items[0]["id"], payment.id)
        self.assertEqual(items[0]["status"], Payment.Status.REFUNDED)
        self.assertEqual(
            items[0]["resolution_summary"]["kind"],
            "refunded_and_cancelled",
        )
        self.assertEqual(
            items[0]["resolution_summary"]["cancelled_as"],
            "professional",
        )
        self.assertEqual(
            items[0]["resolution_summary"]["appointment_status_after"],
            Appointment.Status.CANCELLED_BY_PROFESSIONAL,
        )
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 2**

```bash id="689j48"
cd backend
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
```

**Rollback Fase 2**

```powershell id="8gbhzz"
Copy-Item "backend\apps\payments\tests_api.py.bak_patient_resolution_legend" "backend\apps\payments\tests_api.py" -Force
```

---

### Fase 3: Mostrar la leyenda precisa en la pantalla del paciente

**Objetivo:** que el historial de citas explique claramente lo que pasó.
**Ruta del Archivo:** `web_frontend/pages/patient/appointments.vue`
**Acción:** hacer los reemplazos siguientes.

#### 3A. Insertar este tipo **encima de** `type PaymentItem = {`

```ts id="tqfpdw"
// --- INICIO CAMBIO NUEVO ---
type PaymentResolutionSummary = {
  kind: string
  message: string
  cancelled_as: string | null
  appointment_status_after: string | null
  refunded_at: string | null
}
// --- FIN CAMBIO NUEVO ---
```

#### 3B. Reemplazar `type PaymentItem = { ... }` por esta versión

```ts id="xaypr0"
// --- INICIO CAMBIO NUEVO ---
type PaymentItem = {
  id: number
  external_reference: string
  amount: string
  currency: string
  status: string
  paid_at: string | null
  created_at: string
  resolution_summary?: PaymentResolutionSummary | null
  appointment: {
    id: number
    status: string
    is_paid: boolean
    scheduled_at: string
    ends_at: string
    modality: string
    price: string
    professional: {
      id: number
      name: string
      specialty: string | null
    }
    patient: {
      id: number
      name: string
    }
  }
}
// --- FIN CAMBIO NUEVO ---
```

#### 3C. Insertar esta función **debajo de** `function formatDateTime(value: string) { ... }`

```ts id="zgia06"
// --- INICIO CAMBIO NUEVO ---
function formatNullableDateTime(value: string | null | undefined) {
  return value ? formatDateTime(value) : '—'
}
// --- FIN CAMBIO NUEVO ---
```

#### 3D. Reemplazar la función `canCancel`

```ts id="9f9p16"
// --- INICIO CAMBIO NUEVO ---
function canCancel(appointment: AppointmentItem) {
  return (
    ['pending_confirmation', 'confirmed'].includes(appointment.status) &&
    !appointment.is_paid
  )
}
// --- FIN CAMBIO NUEVO ---
```

#### 3E. Insertar este bloque **debajo de** `const paymentsByAppointment = computed<Record<number, PaymentItem>>(() => { ... })`

```ts id="3f5c6j"
// --- INICIO CAMBIO NUEVO ---
function paymentResolutionSummary(appointment: AppointmentItem) {
  return paymentsByAppointment.value[appointment.id]?.resolution_summary || null
}

function shouldShowResolutionBanner(appointment: AppointmentItem) {
  const payment = paymentsByAppointment.value[appointment.id]
  if (!payment) return false

  return Boolean(payment.resolution_summary) || payment.status === 'refunded'
}

function patientResolutionMessage(appointment: AppointmentItem) {
  const payment = paymentsByAppointment.value[appointment.id]
  const summary = paymentResolutionSummary(appointment)

  if (summary?.message) {
    return summary.message
  }

  if (payment?.status === 'refunded') {
    return 'El pago de esta cita fue reembolsado administrativamente.'
  }

  if (appointment.is_paid) {
    return 'Si la cita ya fue pagada, la resolución posterior depende del flujo administrativo.'
  }

  return ''
}

function showGenericPaidHint(appointment: AppointmentItem) {
  return appointment.is_paid && !shouldShowResolutionBanner(appointment)
}
// --- FIN CAMBIO NUEVO ---
```

#### 3F. Dentro del bloque de “Pago asociado”, **después** del último `<div>` de “Pagado en”, insertar:

```vue id="djlwmr"
<!-- --- INICIO CAMBIO NUEVO --- -->
              <v-alert
                v-if="shouldShowResolutionBanner(appointment)"
                type="info"
                variant="tonal"
                class="mt-4"
              >
                <div class="font-weight-medium">
                  {{ patientResolutionMessage(appointment) }}
                </div>

                <div
                  v-if="paymentResolutionSummary(appointment)?.refunded_at"
                  class="text-caption mt-1"
                >
                  Reembolso registrado:
                  {{ formatNullableDateTime(paymentResolutionSummary(appointment)?.refunded_at) }}
                </div>
              </v-alert>
<!-- --- FIN CAMBIO NUEVO --- -->
```

#### 3G. Reemplazar el `v-if` del mensaje genérico del footer

**Buscar**

```vue id="cqjogt"
v-if="appointment.is_paid"
```

**Reemplazar por**

```vue id="3c6jvp"
v-if="showGenericPaidHint(appointment)"
```

#### 3H. Reemplazar el texto del footer por esta versión

```vue id="lxjfj2"
<!-- --- INICIO CAMBIO NUEVO --- -->
              Si la cita ya fue pagada y aún no aparece una resolución, el siguiente paso depende del flujo administrativo.
<!-- --- FIN CAMBIO NUEVO --- -->
```

**Prueba de Fase 3:**

* Si el pago está `refunded` y trae `resolution_summary`, la tarjeta debe mostrar un `v-alert` informativo.
* Si la cita sigue pagada y todavía no hay resolución específica, debe mantenerse el mensaje genérico.
* El botón de cancelar ya no debe mostrarse en citas pagadas.

**Rollback Fase 3**

```powershell id="hzr5ft"
Copy-Item "web_frontend\pages\patient\appointments.vue.bak_patient_resolution_legend" "web_frontend\pages\patient\appointments.vue" -Force
```

---

## 🧪 Pruebas de Regresión

```bash id="n3m7m5"
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api_lifecycle
```

Luego validar en frontend:

```bash id="53y4a5"
cd web_frontend
npm run dev
```

Validaciones manuales:

* Pago `refunded` + resolución administrativa → debe salir una leyenda precisa.
* Pago `refunded` sin metadata específica → debe salir una leyenda genérica de reembolso.
* Cita `is_paid = true` sin resolución → debe seguir mostrando el aviso genérico.
* Cita pagada → no debe mostrar botón **Cancelar cita**.

---

## 📉 Plan de Reversión Total

```powershell id="a9do7v"
Copy-Item "backend\apps\payments\api_views.py.bak_patient_resolution_legend" "backend\apps\payments\api_views.py" -Force
Copy-Item "backend\apps\payments\tests_api.py.bak_patient_resolution_legend" "backend\apps\payments\tests_api.py" -Force
Copy-Item "web_frontend\pages\patient\appointments.vue.bak_patient_resolution_legend" "web_frontend\pages\patient\appointments.vue" -Force
```

```bash id="ka7kzz"
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
```

El siguiente paso natural sería **mostrar en el panel del profesional una leyenda equivalente cuando la administración haya resuelto una cita pagada que originalmente estaba bloqueando su cancelación**.


-----


---
## ⚠️ Análisis de Impacto y Riesgos

**Módulos afectados:**

* `backend/apps/appointments/api_views.py`
* `backend/apps/appointments/tests_api_lifecycle.py`
* `web_frontend/pages/professional/appointments.vue`

**Riesgos detectados:**

* La pantalla del profesional hoy consume solo `GET /api/v1/professional/appointments` y muestra un mensaje genérico cuando la cita está pagada; no recibe un resumen explícito de resolución administrativa.  
* El backend ya bloquea la cancelación de citas pagadas desde el flujo profesional y remite a reembolso o ajuste administrativo, pero esa información no queda clara en el historial visible del profesional. 
* La tabla `payments_payment` ya tiene `status`, `paid_at` y `raw_response`, por lo que no hace falta migración nueva para mostrar una leyenda derivada. 

**Supuestos:**

* La resolución administrativa deja el pago en `refunded`, y cuando existe metadata adicional en `raw_response`, se la debe resumir de forma segura.
* No se expondrá `raw_response` completo al profesional.
* No se tocarán reglas de negocio de cancelación, solo visibilidad y trazabilidad.

---

## 🛠️ Preparación y Backups

```powershell
# --- INICIO CAMBIO NUEVO ---
Copy-Item "backend\apps\appointments\api_views.py" "backend\apps\appointments\api_views.py.bak_professional_resolution_legend"
Copy-Item "backend\apps\appointments\tests_api_lifecycle.py" "backend\apps\appointments\tests_api_lifecycle.py.bak_professional_resolution_legend"
Copy-Item "web_frontend\pages\professional\appointments.vue" "web_frontend\pages\professional\appointments.vue.bak_professional_resolution_legend"
# --- FIN CAMBIO NUEVO ---
```

```bash
# --- INICIO CAMBIO NUEVO ---
cd backend
..\.venv\Scripts\python.exe manage.py check
# --- FIN CAMBIO NUEVO ---
```

---

## 🚀 Implementación por Fases

### Fase 1: Exponer resumen seguro de resolución en citas del profesional

**Objetivo:** que `/professional/appointments` entregue un campo `resolution_summary` sin abrir ni exponer datos internos completos.
**Ruta del Archivo:** `backend/apps/appointments/api_views.py`
**Acción:** **Insertar y reemplazar bloques puntuales**

### 1A. Agregar este import

**Buscar:**

```python
from django.http import JsonResponse
```

**Reemplazar por:**

```python
# --- INICIO CAMBIO NUEVO ---
from django.http import JsonResponse
from django.core.exceptions import ObjectDoesNotExist
# --- FIN CAMBIO NUEVO ---
```

### 1B. Reemplazar la función `_serialize_appointment` completa por este bloque

**Buscar:**

```python
def _serialize_appointment(app: Appointment):
    return {
        "id": app.id,
        "professional": {
            "id": app.professional_id,
            "name": f"{app.professional.user.first_name} {app.professional.user.last_name}",
            "specialty": app.professional.specialty.name if app.professional.specialty else None,
        },
        "patient": {
            "id": app.patient_id,
            "name": f"{app.patient.user.first_name} {app.patient.user.last_name}",
        },
        "scheduled_at": app.scheduled_at.isoformat(),
        "ends_at": app.ends_at.isoformat(),
        "modality": app.modality,
        "status": app.status,
        "notes": app.notes,
        "price": str(app.price),
        "is_paid": app.is_paid,
    }
```

**Reemplazar por:**

```python
# --- INICIO CAMBIO NUEVO ---
def _build_appointment_resolution_summary(app: Appointment):
    try:
        payment = app.payment
    except ObjectDoesNotExist:
        return None

    raw = payment.raw_response or {}
    decision = str(raw.get("decision") or "").strip()
    cancel_as = str(raw.get("cancel_as") or "").strip().lower() or None
    refunded_at = raw.get("refunded_at")
    appointment_status_after = raw.get("appointment_status_after") or app.status

    if payment.status != "refunded":
        return None

    if decision == "refund_and_cancel_appointment":
        if cancel_as == "professional":
            message = (
                "Administración resolvió esta cita con reembolso y la dejó "
                "cancelada como cancelación del profesional."
            )
        elif cancel_as == "patient":
            message = (
                "Administración resolvió esta cita con reembolso y la dejó "
                "cancelada como cancelación del paciente."
            )
        else:
            message = (
                "Administración resolvió esta cita con reembolso y la dejó cancelada."
            )

        return {
            "kind": "refunded_and_cancelled",
            "message": message,
            "cancelled_as": cancel_as,
            "appointment_status_after": appointment_status_after,
            "refunded_at": refunded_at,
        }

    if decision == "refunded":
        return {
            "kind": "refund_only",
            "message": "Administración reembolsó el pago de esta cita.",
            "cancelled_as": None,
            "appointment_status_after": appointment_status_after,
            "refunded_at": refunded_at,
        }

    return {
        "kind": "refunded",
        "message": "Esta cita figura con pago reembolsado.",
        "cancelled_as": None,
        "appointment_status_after": appointment_status_after,
        "refunded_at": refunded_at,
    }


def _serialize_appointment(app: Appointment):
    return {
        "id": app.id,
        "professional": {
            "id": app.professional_id,
            "name": f"{app.professional.user.first_name} {app.professional.user.last_name}",
            "specialty": app.professional.specialty.name if app.professional.specialty else None,
        },
        "patient": {
            "id": app.patient_id,
            "name": f"{app.patient.user.first_name} {app.patient.user.last_name}",
        },
        "scheduled_at": app.scheduled_at.isoformat(),
        "ends_at": app.ends_at.isoformat(),
        "modality": app.modality,
        "status": app.status,
        "notes": app.notes,
        "price": str(app.price),
        "is_paid": app.is_paid,
        "resolution_summary": _build_appointment_resolution_summary(app),
    }
# --- FIN CAMBIO NUEVO ---
```

### 1C. Reemplazar la función `professional_appointments_collection_view`

**Buscar:**

```python
@require_http_methods(["GET"])
@api_roles_required("professional")
def professional_appointments_collection_view(request):
    professional = request.api_user.professional_profile
    qs = Appointment.objects.filter(professional=professional).select_related(
        "patient__user"
    ).order_by("-scheduled_at")

    return JsonResponse(
        {"items": [_serialize_appointment(a) for a in qs]},
        status=200,
    )
```

**Reemplazar por:**

```python
# --- INICIO CAMBIO NUEVO ---
@require_http_methods(["GET"])
@api_roles_required("professional")
def professional_appointments_collection_view(request):
    professional = request.api_user.professional_profile
    qs = Appointment.objects.filter(professional=professional).select_related(
        "patient__user",
        "professional__user",
        "professional__specialty",
        "payment",
    ).order_by("-scheduled_at")

    return JsonResponse(
        {"items": [_serialize_appointment(a) for a in qs]},
        status=200,
    )
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 1:**
El endpoint `GET /api/v1/professional/appointments` debe seguir devolviendo `items`, pero ahora cada cita podrá incluir `resolution_summary` cuando exista un pago reembolsado.

**Rollback Fase 1:**

```powershell
# --- INICIO CAMBIO NUEVO ---
Copy-Item "backend\apps\appointments\api_views.py.bak_professional_resolution_legend" "backend\apps\appointments\api_views.py" -Force
# --- FIN CAMBIO NUEVO ---
```

---

### Fase 2: Blindar el backend con prueba automática

**Objetivo:** validar que el profesional vea la resolución administrativa en su listado de citas.
**Ruta del Archivo:** `backend/apps/appointments/tests_api_lifecycle.py`
**Acción:** **Agregar import y nuevo test**

### 2A. Agregar import

**Buscar:**

```python
from apps.professionals.models import ProfessionalProfile
```

**Reemplazar por:**

```python
# --- INICIO CAMBIO NUEVO ---
from apps.professionals.models import ProfessionalProfile
from apps.payments.models import Payment
# --- FIN CAMBIO NUEVO ---
```

### 2B. Agregar este test al final de la clase `ProfessionalAppointmentLifecycleApiTests`

```python
# --- INICIO CAMBIO NUEVO ---
    def test_professional_list_includes_resolution_summary_for_refunded_appointment(self):
        Payment.objects.create(
            appointment=self.confirmed_paid_appointment,
            external_reference="demo_reference_040",
            amount=self.confirmed_paid_appointment.price,
            currency="USD",
            status=Payment.Status.REFUNDED,
            paid_at=timezone.now(),
            raw_response={
                "decision": "refund_and_cancel_appointment",
                "cancel_as": "professional",
                "refunded_at": timezone.now().isoformat(),
                "appointment_status_after": Appointment.Status.CANCELLED_BY_PROFESSIONAL,
            },
        )

        self.confirmed_paid_appointment.status = Appointment.Status.CANCELLED_BY_PROFESSIONAL
        self.confirmed_paid_appointment.is_paid = False
        self.confirmed_paid_appointment.save(update_fields=["status", "is_paid"])

        response = self.client.get(
            "/api/v1/professional/appointments",
            **self._auth_headers(self.prof_token),
        )
        self.assertEqual(response.status_code, 200)

        items = response.json()["items"]
        target = [item for item in items if item["id"] == self.confirmed_paid_appointment.id][0]

        self.assertEqual(
            target["resolution_summary"]["kind"],
            "refunded_and_cancelled",
        )
        self.assertEqual(
            target["resolution_summary"]["cancelled_as"],
            "professional",
        )
        self.assertEqual(
            target["resolution_summary"]["appointment_status_after"],
            Appointment.Status.CANCELLED_BY_PROFESSIONAL,
        )
# --- FIN CAMBIO NUEVO ---
```

**Prueba de Fase 2:**

```bash
# --- INICIO CAMBIO NUEVO ---
cd backend
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api_lifecycle
# --- FIN CAMBIO NUEVO ---
```

**Rollback Fase 2:**

```powershell
# --- INICIO CAMBIO NUEVO ---
Copy-Item "backend\apps\appointments\tests_api_lifecycle.py.bak_professional_resolution_legend" "backend\apps\appointments\tests_api_lifecycle.py" -Force
# --- FIN CAMBIO NUEVO ---
```

---

### Fase 3: Mostrar la leyenda precisa en la web del profesional

**Objetivo:** que el profesional deje de ver solo un texto genérico y vea qué resolvió la administración.
**Ruta del Archivo:** `web_frontend/pages/professional/appointments.vue`
**Acción:** **Reemplazar e insertar bloques**

### 3A. Insertar este tipo **encima de** `type AppointmentItem = {`

```ts
// --- INICIO CAMBIO NUEVO ---
type AppointmentResolutionSummary = {
  kind: string
  message: string
  cancelled_as: string | null
  appointment_status_after: string | null
  refunded_at: string | null
}
// --- FIN CAMBIO NUEVO ---
```

### 3B. Reemplazar `type AppointmentItem = { ... }` por esta versión

```ts
// --- INICIO CAMBIO NUEVO ---
type AppointmentItem = {
  id: number
  scheduled_at: string
  ends_at: string
  modality: string
  status: string
  notes: string
  price: string
  is_paid: boolean
  resolution_summary: AppointmentResolutionSummary | null
  patient: {
    id: number
    name: string
  }
  professional: {
    id: number
    name: string
    specialty: string | null
  }
}
// --- FIN CAMBIO NUEVO ---
```

### 3C. Debajo de `function formatDateTime(value: string) { ... }`, insertar:

```ts
// --- INICIO CAMBIO NUEVO ---
function formatNullableDateTime(value: string | null | undefined) {
  return value ? formatDateTime(value) : '—'
}
// --- FIN CAMBIO NUEVO ---
```

### 3D. Debajo de `function canMarkNoShow(appointment: AppointmentItem) { ... }`, insertar:

```ts
// --- INICIO CAMBIO NUEVO ---
function shouldShowResolutionBanner(appointment: AppointmentItem) {
  return Boolean(appointment.resolution_summary)
}

function showGenericPaidHint(appointment: AppointmentItem) {
  return (
    appointment.is_paid &&
    ['pending_confirmation', 'confirmed'].includes(appointment.status) &&
    !appointment.resolution_summary
  )
}
// --- FIN CAMBIO NUEVO ---
```

### 3E. Dentro de `<v-card-text>`, **después** del bloque `<v-row>...</v-row>`, insertar:

```vue
<!-- --- INICIO CAMBIO NUEVO --- -->
            <v-alert
              v-if="shouldShowResolutionBanner(appointment)"
              type="info"
              variant="tonal"
              class="mt-4"
            >
              <div class="font-weight-medium">
                {{ appointment.resolution_summary?.message }}
              </div>

              <div
                v-if="appointment.resolution_summary?.refunded_at"
                class="text-caption mt-1"
              >
                Reembolso registrado:
                {{ formatNullableDateTime(appointment.resolution_summary?.refunded_at) }}
              </div>
            </v-alert>
<!-- --- FIN CAMBIO NUEVO --- -->
```

### 3F. Reemplazar el `v-if` del texto genérico del footer

**Buscar:**

```vue
v-if="appointment.is_paid && ['pending_confirmation', 'confirmed'].includes(appointment.status)"
```

**Reemplazar por:**

```vue
v-if="showGenericPaidHint(appointment)"
```

### 3G. Reemplazar el texto genérico

**Buscar:**

```vue
La cancelación de una cita pagada queda bloqueada hasta implementar reembolso.
```

**Reemplazar por:**

```vue
La cancelación de una cita pagada queda bloqueada hasta que exista una resolución administrativa.
```

**Prueba de Fase 3:**

* Una cita con `resolution_summary` debe mostrar un `v-alert` con el mensaje preciso.
* Una cita pagada sin `resolution_summary` debe mantener el texto genérico.
* Una cita ya resuelta administrativamente no debe depender solo del footer.

**Rollback Fase 3:**

```powershell
# --- INICIO CAMBIO NUEVO ---
Copy-Item "web_frontend\pages\professional\appointments.vue.bak_professional_resolution_legend" "web_frontend\pages\professional\appointments.vue" -Force
# --- FIN CAMBIO NUEVO ---
```

---

## 🧪 Pruebas de Regresión

```bash
# --- INICIO CAMBIO NUEVO ---
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api_lifecycle
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api
..\.venv\Scripts\python.exe manage.py test apps.payments.tests_api
# --- FIN CAMBIO NUEVO ---
```

```bash
# --- INICIO CAMBIO NUEVO ---
cd web_frontend
npm run dev
# --- FIN CAMBIO NUEVO ---
```

Validaciones manuales:

* Cita pagada activa sin resolución: debe seguir mostrando el aviso genérico.
* Cita con pago `refunded` y metadata de resolución: debe mostrar la leyenda precisa.
* Cita reembolsada y cancelada por administración como profesional: la leyenda debe indicarlo claramente.
* El botón **Cancelar** debe seguir oculto cuando `is_paid = true`, como ya ocurre hoy. 

---

## 📉 Plan de Reversión Total

```powershell
# --- INICIO CAMBIO NUEVO ---
Copy-Item "backend\apps\appointments\api_views.py.bak_professional_resolution_legend" "backend\apps\appointments\api_views.py" -Force
Copy-Item "backend\apps\appointments\tests_api_lifecycle.py.bak_professional_resolution_legend" "backend\apps\appointments\tests_api_lifecycle.py" -Force
Copy-Item "web_frontend\pages\professional\appointments.vue.bak_professional_resolution_legend" "web_frontend\pages\professional\appointments.vue" -Force
# --- FIN CAMBIO NUEVO ---
```

```bash
# --- INICIO CAMBIO NUEVO ---
cd backend
..\.venv\Scripts\python.exe manage.py check
..\.venv\Scripts\python.exe manage.py test apps.appointments.tests_api_lifecycle
# --- FIN CAMBIO NUEVO ---
```

El siguiente paso natural después de este sería **mostrar en la bandeja administrativa de pagos una referencia navegable hacia la cita afectada**, para que el operador vea el impacto clínico y financiero desde un solo punto.




