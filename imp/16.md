#### Impacto y riesgos

El siguiente paso más seguro y coherente en la secuencia es **exponer la auditoría por API**.

Motivo técnico conservador:

* el modelo `AuditEvent` ya existe;
* varias capas ya están escribiendo eventos de auditoría;
* todavía no hay una API para consultar esa trazabilidad;
* esto desbloquea el panel administrativo y el panel de privacidad sin tocar tablas ni reglas sensibles de negocio.

Riesgos controlados:

* **No hay cambio de base de datos**.
* **No se altera autenticación**.
* **No se toca reserva, pagos ni validación documental**.
* El único riesgo real es de **consultas pesadas** si la tabla crece mucho. Por eso el código propuesto ya deja:

  * paginación;
  * tamaño máximo de página;
  * exportación CSV con tope.

Supuesto conservador:

* el frontend todavía no está en este repo o no es el siguiente bloque obligatorio;
* por eso primero se cierra el backend faltante que soporta privacidad, soporte y trazabilidad.

---

#### Preparación

Antes de pegar código:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py showmigrations
```

Backup de base, aunque no haya migración:

```bash
docker exec -t ubemedicos_postgres pg_dump -U appuser -d appdb > backup_pre_audits_api.sql
```

Backup simple de archivos que se van a tocar:

```powershell
cd G:\codex_projects\ubemedicos
copy backend\config\urls.py backend\config\urls.py.bak
```

No se necesita migración nueva.

---

#### Fase 1

**Objetivo**

Crear la API de auditoría para:

* listar logs de auditoría para admin, super_admin y privacy_auditor;
* ver detalle de un evento;
* exportar CSV;
* permitir que cualquier usuario autenticado vea solo sus propios eventos.

**Riesgo controlado**

Se agrega una capa de solo lectura sobre datos que ya existen. No se altera ninguna lógica de escritura previa.

**Archivos a tocar**

1. `backend/apps/audits/api_views.py` → **agregar archivo nuevo**
2. `backend/apps/audits/api_urls.py` → **agregar archivo nuevo**
3. `backend/config/urls.py` → **reemplazar archivo completo**

---

##### Archivo: `backend/apps/audits/api_views.py`

```python
# INICIO CAMBIO NUEVO
import csv
import json
from datetime import datetime, time

from django.http import HttpResponse, JsonResponse
from django.utils import timezone
from django.utils.dateparse import parse_date, parse_datetime
from django.views.decorators.http import require_http_methods

from apps.users.api_auth import api_auth_required, api_roles_required
from .models import AuditEvent


MAX_PAGE_SIZE = 200
MAX_EXPORT_ROWS = 5000


def _json_error(message, status=400, extra=None):
    payload = {"detail": message}
    if extra:
        payload.update(extra)
    return JsonResponse(payload, status=status)


def _serialize_actor(actor):
    if actor is None:
        return None

    return {
        "id": actor.id,
        "email": actor.email,
        "role": actor.role,
        "first_name": actor.first_name,
        "last_name": actor.last_name,
    }


def _serialize_event(event: AuditEvent):
    return {
        "id": event.id,
        "actor": _serialize_actor(event.actor),
        "event_type": event.event_type,
        "entity_type": event.entity_type,
        "entity_id": event.entity_id,
        "metadata": event.metadata or {},
        "created_at": event.created_at.isoformat() if event.created_at else None,
    }


def _parse_positive_int(raw_value, *, default, field_name, min_value=1, max_value=None):
    if raw_value in (None, ""):
        return default

    try:
        value = int(raw_value)
    except (TypeError, ValueError):
        raise ValueError(f"{field_name} debe ser numérico.")

    if value < min_value:
        raise ValueError(f"{field_name} debe ser mayor o igual a {min_value}.")

    if max_value is not None and value > max_value:
        raise ValueError(f"{field_name} no puede ser mayor a {max_value}.")

    return value


def _coerce_datetime(value, *, field_name, end_of_day=False):
    if not value:
        return None

    parsed_dt = parse_datetime(value)
    if parsed_dt is not None:
        if timezone.is_naive(parsed_dt):
            parsed_dt = timezone.make_aware(
                parsed_dt,
                timezone.get_current_timezone(),
            )
        return parsed_dt

    parsed_date = parse_date(value)
    if parsed_date is not None:
        selected_time = (
            time(23, 59, 59, 999999) if end_of_day else time(0, 0, 0)
        )
        combined = datetime.combine(parsed_date, selected_time)
        return timezone.make_aware(
            combined,
            timezone.get_current_timezone(),
        )

    raise ValueError(
        f"{field_name} debe venir en formato ISO datetime o YYYY-MM-DD."
    )


def _build_filtered_queryset(request):
    qs = AuditEvent.objects.select_related("actor").all().order_by("-created_at", "-id")

    actor_id = request.GET.get("actor_id")
    actor_email = (request.GET.get("actor_email") or "").strip()
    event_type = (request.GET.get("event_type") or "").strip()
    entity_type = (request.GET.get("entity_type") or "").strip()
    entity_id = (request.GET.get("entity_id") or "").strip()
    created_from = (request.GET.get("created_from") or "").strip()
    created_to = (request.GET.get("created_to") or "").strip()

    if actor_id:
        try:
            actor_id = int(actor_id)
        except (TypeError, ValueError):
            raise ValueError("actor_id debe ser numérico.")
        qs = qs.filter(actor_id=actor_id)

    if actor_email:
        qs = qs.filter(actor__email__icontains=actor_email)

    if event_type:
        qs = qs.filter(event_type=event_type)

    if entity_type:
        qs = qs.filter(entity_type=entity_type)

    if entity_id:
        qs = qs.filter(entity_id=entity_id)

    if created_from:
        qs = qs.filter(
            created_at__gte=_coerce_datetime(
                created_from,
                field_name="created_from",
                end_of_day=False,
            )
        )

    if created_to:
        qs = qs.filter(
            created_at__lte=_coerce_datetime(
                created_to,
                field_name="created_to",
                end_of_day=True,
            )
        )

    return qs


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_audit_logs_collection_view(request):
    try:
        qs = _build_filtered_queryset(request)
        page = _parse_positive_int(
            request.GET.get("page"),
            default=1,
            field_name="page",
            min_value=1,
        )
        page_size = _parse_positive_int(
            request.GET.get("page_size"),
            default=50,
            field_name="page_size",
            min_value=1,
            max_value=MAX_PAGE_SIZE,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    total = qs.count()
    offset = (page - 1) * page_size
    items = list(qs[offset : offset + page_size])

    return JsonResponse(
        {
            "items": [_serialize_event(event) for event in items],
            "pagination": {
                "page": page,
                "page_size": page_size,
                "total": total,
                "has_next": offset + page_size < total,
            },
            "filters": {
                "actor_id": request.GET.get("actor_id"),
                "actor_email": request.GET.get("actor_email"),
                "event_type": request.GET.get("event_type"),
                "entity_type": request.GET.get("entity_type"),
                "entity_id": request.GET.get("entity_id"),
                "created_from": request.GET.get("created_from"),
                "created_to": request.GET.get("created_to"),
            },
        },
        status=200,
    )


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_audit_log_detail_view(request, event_id: int):
    event = AuditEvent.objects.select_related("actor").filter(id=event_id).first()
    if not event:
        return _json_error("Evento de auditoría no encontrado.", status=404)

    return JsonResponse(_serialize_event(event), status=200)


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_audit_logs_export_view(request):
    try:
        qs = _build_filtered_queryset(request)
        limit = _parse_positive_int(
            request.GET.get("limit"),
            default=MAX_EXPORT_ROWS,
            field_name="limit",
            min_value=1,
            max_value=MAX_EXPORT_ROWS,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    rows = list(qs[:limit])

    response = HttpResponse(content_type="text/csv; charset=utf-8")
    response["Content-Disposition"] = 'attachment; filename="audit_logs.csv"'

    writer = csv.writer(response)
    writer.writerow(
        [
            "id",
            "created_at",
            "actor_id",
            "actor_email",
            "actor_role",
            "event_type",
            "entity_type",
            "entity_id",
            "metadata_json",
        ]
    )

    for event in rows:
        writer.writerow(
            [
                event.id,
                event.created_at.isoformat() if event.created_at else "",
                event.actor_id or "",
                event.actor.email if event.actor else "",
                event.actor.role if event.actor else "",
                event.event_type,
                event.entity_type,
                event.entity_id,
                json.dumps(event.metadata or {}, ensure_ascii=False),
            ]
        )

    response["X-Export-Row-Limit"] = str(limit)
    response["X-Export-Returned-Rows"] = str(len(rows))
    return response


@require_http_methods(["GET"])
@api_auth_required
def my_audit_events_view(request):
    try:
        page = _parse_positive_int(
            request.GET.get("page"),
            default=1,
            field_name="page",
            min_value=1,
        )
        page_size = _parse_positive_int(
            request.GET.get("page_size"),
            default=20,
            field_name="page_size",
            min_value=1,
            max_value=MAX_PAGE_SIZE,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    qs = AuditEvent.objects.select_related("actor").filter(actor=request.api_user)

    event_type = (request.GET.get("event_type") or "").strip()
    entity_type = (request.GET.get("entity_type") or "").strip()

    if event_type:
        qs = qs.filter(event_type=event_type)

    if entity_type:
        qs = qs.filter(entity_type=entity_type)

    qs = qs.order_by("-created_at", "-id")

    total = qs.count()
    offset = (page - 1) * page_size
    items = list(qs[offset : offset + page_size])

    return JsonResponse(
        {
            "items": [_serialize_event(event) for event in items],
            "pagination": {
                "page": page,
                "page_size": page_size,
                "total": total,
                "has_next": offset + page_size < total,
            },
        },
        status=200,
    )
# FIN CAMBIO NUEVO
```

---

##### Archivo: `backend/apps/audits/api_urls.py`

```python
# INICIO CAMBIO NUEVO
from django.urls import path

from .api_views import (
    admin_audit_logs_collection_view,
    admin_audit_log_detail_view,
    admin_audit_logs_export_view,
    my_audit_events_view,
)

urlpatterns = [
    path(
        "admin/audits/access-logs",
        admin_audit_logs_collection_view,
        name="admin-audit-logs-collection",
    ),
    path(
        "admin/audits/access-logs/export.csv",
        admin_audit_logs_export_view,
        name="admin-audit-logs-export",
    ),
    path(
        "admin/audits/access-logs/<int:event_id>",
        admin_audit_log_detail_view,
        name="admin-audit-logs-detail",
    ),
    path(
        "users/me/audit-events",
        my_audit_events_view,
        name="users-me-audit-events",
    ),
]
# FIN CAMBIO NUEVO
```

---

##### Archivo: `backend/config/urls.py`

```python
# INICIO CAMBIO NUEVO
from django.contrib import admin
from django.urls import include, path

urlpatterns = [
    path("admin/", admin.site.urls),
    path("api/v1/", include("apps.users.api_urls")),
    path("api/v1/", include("apps.appointments.api_urls")),
    path("api/v1/", include("apps.professionals.api_urls")),
    path("api/v1/", include("apps.payments.api_urls")),
    path("api/v1/", include("apps.audits.api_urls")),
]
# FIN CAMBIO NUEVO
```

---

#### Prueba de Fase 1

**Cómo probar**

Validación de sintaxis y proyecto:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
```

Levantar servidor:

```bash
python manage.py runserver
```

Login admin para obtener token:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"admin@demo.local\",\"password\":\"Admin123!\"}"
```

Listar logs:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs?page=1&page_size=20" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Filtrar por tipo de evento:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs?event_type=payment_intent_created" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Ver detalle:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs/1" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Exportar CSV:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs/export.csv?limit=100" ^
  -H "Authorization: Bearer TOKEN_ADMIN" ^
  --output audit_logs.csv
```

Ver auditoría propia del usuario autenticado:

```bash
curl "http://127.0.0.1:8000/api/v1/users/me/audit-events" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

**Qué debe seguir funcionando**

* login;
* registro de paciente;
* registro de profesional;
* disponibilidad del profesional;
* reserva y cancelación de citas;
* validación documental;
* pagos demo.

**Cómo revertir esta fase**

1. Eliminar:

   * `backend/apps/audits/api_views.py`
   * `backend/apps/audits/api_urls.py`

2. Restaurar `backend/config/urls.py` desde backup:

```powershell
copy /Y backend\config\urls.py.bak backend\config\urls.py
```

3. Validar otra vez:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
```

---

#### Fase 2

**Objetivo**

Dejar pruebas automáticas del bloque nuevo para que el programador no dependa solo de pruebas manuales.

**Riesgo controlado**

Se reduce el riesgo de romper control de acceso, filtros y exportación al integrar cambios futuros.

**Archivo a tocar**

`backend/apps/audits/tests_api.py` → **agregar archivo nuevo**

---

##### Archivo: `backend/apps/audits/tests_api.py`

```python
# INICIO CAMBIO NUEVO
import json

from django.contrib.auth import get_user_model
from django.test import TestCase

from .models import AuditEvent


class AuditApiTests(TestCase):
    def setUp(self):
        User = get_user_model()

        self.admin_user = User.objects.create_user(
            email="admin.audit@test.local",
            password="Admin123!",
            first_name="Admin",
            last_name="Audit",
            role="admin",
            is_staff=True,
        )

        self.super_admin_user = User.objects.create_user(
            email="super.audit@test.local",
            password="Admin123!",
            first_name="Super",
            last_name="Audit",
            role="super_admin",
            is_staff=True,
            is_superuser=True,
        )

        self.privacy_user = User.objects.create_user(
            email="privacy.audit@test.local",
            password="Privacy123!",
            first_name="Privacy",
            last_name="Auditor",
            role="privacy_auditor",
        )

        self.professional_user = User.objects.create_user(
            email="professional.audit@test.local",
            password="Profesional123!",
            first_name="Doctor",
            last_name="Audit",
            role="professional",
        )

        self.patient_user = User.objects.create_user(
            email="patient.audit@test.local",
            password="Paciente123!",
            first_name="Paciente",
            last_name="Audit",
            role="patient",
        )

        self.admin_token = self._login("admin.audit@test.local", "Admin123!")
        self.super_admin_token = self._login("super.audit@test.local", "Admin123!")
        self.privacy_token = self._login("privacy.audit@test.local", "Privacy123!")
        self.prof_token = self._login(
            "professional.audit@test.local",
            "Profesional123!",
        )
        self.patient_token = self._login("patient.audit@test.local", "Paciente123!")

        self.event_admin = AuditEvent.objects.create(
            actor=self.admin_user,
            event_type="availability_slot_created",
            entity_type="AvailabilitySlot",
            entity_id="10",
            metadata={"weekday": 1, "modality": "in_person"},
        )

        self.event_patient = AuditEvent.objects.create(
            actor=self.patient_user,
            event_type="payment_intent_created",
            entity_type="Payment",
            entity_id="20",
            metadata={"amount": "25.00", "currency": "USD"},
        )

        self.event_professional = AuditEvent.objects.create(
            actor=self.professional_user,
            event_type="professional_verification_submitted",
            entity_type="ProfessionalVerificationSubmission",
            entity_id="30",
            metadata={"professional_id": 99},
        )

        self.event_system = AuditEvent.objects.create(
            actor=None,
            event_type="system_seed",
            entity_type="SeedCommand",
            entity_id="seed-1",
            metadata={"source": "seed_initial_data"},
        )

    def _login(self, email, password):
        response = self.client.post(
            "/api/v1/auth/login",
            data=json.dumps({"email": email, "password": password}),
            content_type="application/json",
        )
        self.assertEqual(response.status_code, 200)
        return response.json()["access_token"]

    def _auth_headers(self, token):
        return {"HTTP_AUTHORIZATION": f"Bearer {token}"}

    def test_admin_can_list_audit_logs(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertIn("items", response.json())
        self.assertGreaterEqual(len(response.json()["items"]), 4)

    def test_super_admin_can_list_audit_logs(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs",
            **self._auth_headers(self.super_admin_token),
        )
        self.assertEqual(response.status_code, 200)

    def test_privacy_auditor_can_list_audit_logs(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs",
            **self._auth_headers(self.privacy_token),
        )
        self.assertEqual(response.status_code, 200)

    def test_professional_cannot_access_admin_audit_logs(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs",
            **self._auth_headers(self.prof_token),
        )
        self.assertEqual(response.status_code, 403)

    def test_admin_can_filter_by_actor_email(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs?actor_email=patient.audit@test.local",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)

        items = response.json()["items"]
        self.assertEqual(len(items), 1)
        self.assertEqual(items[0]["actor"]["email"], "patient.audit@test.local")
        self.assertEqual(items[0]["event_type"], "payment_intent_created")

    def test_admin_can_filter_by_event_type(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs?event_type=availability_slot_created",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)

        items = response.json()["items"]
        self.assertEqual(len(items), 1)
        self.assertEqual(items[0]["id"], self.event_admin.id)

    def test_admin_can_view_audit_log_detail(self):
        response = self.client.get(
            f"/api/v1/admin/audits/access-logs/{self.event_patient.id}",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertEqual(response.json()["id"], self.event_patient.id)
        self.assertEqual(response.json()["entity_type"], "Payment")

    def test_admin_can_export_csv(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs/export.csv?limit=3",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertIn("text/csv", response["Content-Type"])
        self.assertEqual(response["X-Export-Row-Limit"], "3")
        self.assertIn("payment_intent_created", response.content.decode("utf-8"))

    def test_me_audit_events_only_returns_current_user_events(self):
        response = self.client.get(
            "/api/v1/users/me/audit-events",
            **self._auth_headers(self.patient_token),
        )
        self.assertEqual(response.status_code, 200)

        items = response.json()["items"]
        self.assertEqual(len(items), 1)
        self.assertEqual(items[0]["actor"]["email"], "patient.audit@test.local")
        self.assertEqual(items[0]["event_type"], "payment_intent_created")

    def test_me_audit_events_requires_auth(self):
        response = self.client.get("/api/v1/users/me/audit-events")
        self.assertEqual(response.status_code, 401)

    def test_invalid_page_size_returns_400(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs?page_size=9999",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 400)
# FIN CAMBIO NUEVO
```

---

#### Prueba de Fase 2

Ejecutar solo el bloque nuevo:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py test apps.audits.tests_api -v 2
```

Debe pasar completo.

**Qué debe seguir funcionando**

* todos los tests de `users`;
* todos los tests de `appointments`;
* todos los tests de `professionals`;
* todos los tests de `payments`.

**Cómo revertir esta fase**

Eliminar:

* `backend/apps/audits/tests_api.py`

No toca base de datos.

---

#### Pruebas de regresión

Ejecutar después de pegar todo:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.appointments.tests_api_booking -v 2
python manage.py test apps.professionals.tests_api -v 2
python manage.py test apps.professionals.tests_api_admin -v 2
python manage.py test apps.payments.tests_api -v 2
python manage.py test apps.audits.tests_api -v 2
```

Rutas nuevas que deben quedar activas:

* `GET /api/v1/admin/audits/access-logs`
* `GET /api/v1/admin/audits/access-logs/<id>`
* `GET /api/v1/admin/audits/access-logs/export.csv`
* `GET /api/v1/users/me/audit-events`

---

#### Plan de reversión total

Como no hay migración ni cambio de tabla, la reversión total es simple.

Paso 1. Eliminar archivos nuevos:

* `backend/apps/audits/api_views.py`
* `backend/apps/audits/api_urls.py`
* `backend/apps/audits/tests_api.py`

Paso 2. Restaurar `backend/config/urls.py` desde backup:

```powershell
copy /Y backend\config\urls.py.bak backend\config\urls.py
```

Paso 3. Validar integridad:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
```

Paso 4. Correr regresión mínima:

```bash
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.professionals.tests_api -v 2
python manage.py test apps.payments.tests_api -v 2
```

Después de este bloque, el siguiente paso natural ya sería **la pantalla web de privacidad/auditoría** consumiendo estas rutas, o el **panel administrativo de trazabilidad** si se quiere seguir cerrando backend + frontend por módulos.

----


#### Impacto y riesgos

El repositorio empacado que se revisó **solo contiene backend Django**. No trae el frontend Nuxt/Vue real, por lo que el siguiente paso implementable sin inventar código fuera del repo es dejar **listo el backend del panel web administrativo y de privacidad**.

El cambio mínimo y conservador que conviene implementar ahora es este:

1. **API de auditoría consultable**:

   * listar logs;
   * ver detalle;
   * exportar CSV;
   * ver auditoría propia del usuario.

2. **API de resumen para panel web**:

   * métricas de usuarios;
   * estado de verificaciones profesionales;
   * citas;
   * pagos;
   * actividad reciente.

Riesgos controlados:

* **No hay migraciones**.
* **No se toca el esquema**.
* **No se modifica autenticación**.
* **No se altera reserva, pagos ni validación documental**.
* El riesgo principal sería hacer consultas administrativas pesadas. Se controla con:

  * paginación;
  * filtros;
  * límite máximo de exportación;
  * límite máximo en actividad reciente.

---

#### Preparación

Antes de pegar código, el programador debe hacer esto:

Backup simple de base:

```bash
docker exec -t ubemedicos_postgres pg_dump -U appuser -d appdb > backup_pre_audits_dashboard_api.sql
```

Backup de archivos que se van a tocar:

```powershell
cd G:\codex_projects\ubemedicos
copy backend\config\urls.py backend\config\urls.py.bak
```

Validación previa:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py showmigrations
```

No debe ejecutarse ninguna migración nueva porque este bloque no cambia tablas.

---

#### Fase 1

**Objetivo**

Dejar publicado el backend que alimentará el panel web administrativo y de privacidad.

**Riesgo controlado**

Se agrega una capa de lectura y resumen sobre modelos ya existentes. No se modifica ninguna lógica crítica actual.

**Archivo a tocar**

1. `backend/apps/audits/api_views.py` → **agregar archivo nuevo**
2. `backend/apps/audits/api_urls.py` → **agregar archivo nuevo**
3. `backend/config/urls.py` → **reemplazar archivo completo**

---

##### Archivo: `backend/apps/audits/api_views.py`

```python
# INICIO CAMBIO NUEVO
import csv
import json
from datetime import datetime, time, timedelta

from django.db.models import Count, Max, Q, Sum
from django.http import HttpResponse, JsonResponse
from django.utils import timezone
from django.utils.dateparse import parse_date, parse_datetime
from django.views.decorators.http import require_http_methods

from apps.appointments.models import Appointment
from apps.payments.models import Payment
from apps.professionals.models import (
    ProfessionalDocument,
    ProfessionalProfile,
    ProfessionalVerificationSubmission,
)
from apps.users.api_auth import api_auth_required, api_roles_required
from apps.users.models import User
from .models import AuditEvent


MAX_PAGE_SIZE = 200
MAX_EXPORT_ROWS = 5000
ACTIVE_APPOINTMENT_STATUSES = [
    Appointment.Status.PENDING_CONFIRMATION,
    Appointment.Status.CONFIRMED,
]


def _json_error(message, status=400, extra=None):
    payload = {"detail": message}
    if extra:
        payload.update(extra)
    return JsonResponse(payload, status=status)


def _serialize_actor(actor):
    if actor is None:
        return None

    return {
        "id": actor.id,
        "email": actor.email,
        "role": actor.role,
        "first_name": actor.first_name,
        "last_name": actor.last_name,
    }


def _serialize_event(event: AuditEvent):
    return {
        "id": event.id,
        "actor": _serialize_actor(event.actor),
        "event_type": event.event_type,
        "entity_type": event.entity_type,
        "entity_id": event.entity_id,
        "metadata": event.metadata or {},
        "created_at": event.created_at.isoformat() if event.created_at else None,
    }


def _parse_positive_int(raw_value, *, default, field_name, min_value=1, max_value=None):
    if raw_value in (None, ""):
        return default

    try:
        value = int(raw_value)
    except (TypeError, ValueError):
        raise ValueError(f"{field_name} debe ser numérico.")

    if value < min_value:
        raise ValueError(f"{field_name} debe ser mayor o igual a {min_value}.")

    if max_value is not None and value > max_value:
        raise ValueError(f"{field_name} no puede ser mayor a {max_value}.")

    return value


def _coerce_datetime(value, *, field_name, end_of_day=False):
    if not value:
        return None

    parsed_dt = parse_datetime(value)
    if parsed_dt is not None:
        if timezone.is_naive(parsed_dt):
            parsed_dt = timezone.make_aware(
                parsed_dt,
                timezone.get_current_timezone(),
            )
        return parsed_dt

    parsed_date = parse_date(value)
    if parsed_date is not None:
        selected_time = time(23, 59, 59, 999999) if end_of_day else time(0, 0, 0)
        combined = datetime.combine(parsed_date, selected_time)
        return timezone.make_aware(
            combined,
            timezone.get_current_timezone(),
        )

    raise ValueError(
        f"{field_name} debe venir en formato ISO datetime o YYYY-MM-DD."
    )


def _build_filtered_queryset(request):
    qs = AuditEvent.objects.select_related("actor").all().order_by("-created_at", "-id")

    actor_id = request.GET.get("actor_id")
    actor_email = (request.GET.get("actor_email") or "").strip()
    event_type = (request.GET.get("event_type") or "").strip()
    entity_type = (request.GET.get("entity_type") or "").strip()
    entity_id = (request.GET.get("entity_id") or "").strip()
    created_from = (request.GET.get("created_from") or "").strip()
    created_to = (request.GET.get("created_to") or "").strip()

    if actor_id:
        try:
            actor_id = int(actor_id)
        except (TypeError, ValueError):
            raise ValueError("actor_id debe ser numérico.")
        qs = qs.filter(actor_id=actor_id)

    if actor_email:
        qs = qs.filter(actor__email__icontains=actor_email)

    if event_type:
        qs = qs.filter(event_type=event_type)

    if entity_type:
        qs = qs.filter(entity_type=entity_type)

    if entity_id:
        qs = qs.filter(entity_id=entity_id)

    if created_from:
        qs = qs.filter(
            created_at__gte=_coerce_datetime(
                created_from,
                field_name="created_from",
                end_of_day=False,
            )
        )

    if created_to:
        qs = qs.filter(
            created_at__lte=_coerce_datetime(
                created_to,
                field_name="created_to",
                end_of_day=True,
            )
        )

    return qs


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_audit_logs_collection_view(request):
    try:
        qs = _build_filtered_queryset(request)
        page = _parse_positive_int(
            request.GET.get("page"),
            default=1,
            field_name="page",
            min_value=1,
        )
        page_size = _parse_positive_int(
            request.GET.get("page_size"),
            default=50,
            field_name="page_size",
            min_value=1,
            max_value=MAX_PAGE_SIZE,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    total = qs.count()
    offset = (page - 1) * page_size
    items = list(qs[offset : offset + page_size])

    return JsonResponse(
        {
            "items": [_serialize_event(event) for event in items],
            "pagination": {
                "page": page,
                "page_size": page_size,
                "total": total,
                "has_next": offset + page_size < total,
            },
            "filters": {
                "actor_id": request.GET.get("actor_id"),
                "actor_email": request.GET.get("actor_email"),
                "event_type": request.GET.get("event_type"),
                "entity_type": request.GET.get("entity_type"),
                "entity_id": request.GET.get("entity_id"),
                "created_from": request.GET.get("created_from"),
                "created_to": request.GET.get("created_to"),
            },
        },
        status=200,
    )


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_audit_log_detail_view(request, event_id: int):
    event = AuditEvent.objects.select_related("actor").filter(id=event_id).first()
    if not event:
        return _json_error("Evento de auditoría no encontrado.", status=404)

    return JsonResponse(_serialize_event(event), status=200)


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_audit_logs_export_view(request):
    try:
        qs = _build_filtered_queryset(request)
        limit = _parse_positive_int(
            request.GET.get("limit"),
            default=MAX_EXPORT_ROWS,
            field_name="limit",
            min_value=1,
            max_value=MAX_EXPORT_ROWS,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    rows = list(qs[:limit])

    response = HttpResponse(content_type="text/csv; charset=utf-8")
    response["Content-Disposition"] = 'attachment; filename="audit_logs.csv"'

    writer = csv.writer(response)
    writer.writerow(
        [
            "id",
            "created_at",
            "actor_id",
            "actor_email",
            "actor_role",
            "event_type",
            "entity_type",
            "entity_id",
            "metadata_json",
        ]
    )

    for event in rows:
        writer.writerow(
            [
                event.id,
                event.created_at.isoformat() if event.created_at else "",
                event.actor_id or "",
                event.actor.email if event.actor else "",
                event.actor.role if event.actor else "",
                event.event_type,
                event.entity_type,
                event.entity_id,
                json.dumps(event.metadata or {}, ensure_ascii=False),
            ]
        )

    response["X-Export-Row-Limit"] = str(limit)
    response["X-Export-Returned-Rows"] = str(len(rows))
    return response


@require_http_methods(["GET"])
@api_auth_required
def my_audit_events_view(request):
    try:
        page = _parse_positive_int(
            request.GET.get("page"),
            default=1,
            field_name="page",
            min_value=1,
        )
        page_size = _parse_positive_int(
            request.GET.get("page_size"),
            default=20,
            field_name="page_size",
            min_value=1,
            max_value=MAX_PAGE_SIZE,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    qs = AuditEvent.objects.select_related("actor").filter(actor=request.api_user)

    event_type = (request.GET.get("event_type") or "").strip()
    entity_type = (request.GET.get("entity_type") or "").strip()

    if event_type:
        qs = qs.filter(event_type=event_type)

    if entity_type:
        qs = qs.filter(entity_type=entity_type)

    qs = qs.order_by("-created_at", "-id")

    total = qs.count()
    offset = (page - 1) * page_size
    items = list(qs[offset : offset + page_size])

    return JsonResponse(
        {
            "items": [_serialize_event(event) for event in items],
            "pagination": {
                "page": page,
                "page_size": page_size,
                "total": total,
                "has_next": offset + page_size < total,
            },
        },
        status=200,
    )


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_dashboard_summary_view(request):
    now = timezone.now()
    local_now = timezone.localtime(now)
    today_start = local_now.replace(hour=0, minute=0, second=0, microsecond=0)
    tomorrow_start = today_start + timedelta(days=1)
    next_7_days = now + timedelta(days=7)

    users_total = User.objects.count()
    users_active_total = User.objects.filter(is_active=True).count()
    users_by_role_qs = User.objects.values("role").annotate(total=Count("id"))
    users_by_role = {row["role"]: row["total"] for row in users_by_role_qs}

    professional_status_qs = ProfessionalProfile.objects.values(
        "verification_status"
    ).annotate(total=Count("id"))
    professional_status = {
        row["verification_status"]: row["total"] for row in professional_status_qs
    }

    payment_stats = Payment.objects.aggregate(
        pending_total=Count("id", filter=Q(status=Payment.Status.PENDING)),
        succeeded_total=Count("id", filter=Q(status=Payment.Status.SUCCEEDED)),
        failed_total=Count("id", filter=Q(status=Payment.Status.FAILED)),
        pending_amount_total=Sum("amount", filter=Q(status=Payment.Status.PENDING)),
        succeeded_amount_total=Sum(
            "amount",
            filter=Q(status=Payment.Status.SUCCEEDED),
        ),
        failed_amount_total=Sum("amount", filter=Q(status=Payment.Status.FAILED)),
    )

    latest_audit = AuditEvent.objects.aggregate(latest_created_at=Max("created_at"))

    return JsonResponse(
        {
            "generated_at": now.isoformat(),
            "users": {
                "total": users_total,
                "active_total": users_active_total,
                "by_role": {
                    User.Role.SUPER_ADMIN: users_by_role.get(User.Role.SUPER_ADMIN, 0),
                    User.Role.ADMIN: users_by_role.get(User.Role.ADMIN, 0),
                    User.Role.PRIVACY_AUDITOR: users_by_role.get(
                        User.Role.PRIVACY_AUDITOR,
                        0,
                    ),
                    User.Role.PROFESSIONAL: users_by_role.get(
                        User.Role.PROFESSIONAL,
                        0,
                    ),
                    User.Role.PATIENT: users_by_role.get(User.Role.PATIENT, 0),
                },
            },
            "professionals": {
                "total": ProfessionalProfile.objects.count(),
                "public_profiles_enabled_total": ProfessionalProfile.objects.filter(
                    public_profile_enabled=True
                ).count(),
                "accepting_patients_total": ProfessionalProfile.objects.filter(
                    is_accepting_patients=True
                ).count(),
                "verification_status": {
                    ProfessionalProfile.VerificationStatus.DRAFT: professional_status.get(
                        ProfessionalProfile.VerificationStatus.DRAFT,
                        0,
                    ),
                    ProfessionalProfile.VerificationStatus.SUBMITTED: professional_status.get(
                        ProfessionalProfile.VerificationStatus.SUBMITTED,
                        0,
                    ),
                    ProfessionalProfile.VerificationStatus.UNDER_REVIEW: professional_status.get(
                        ProfessionalProfile.VerificationStatus.UNDER_REVIEW,
                        0,
                    ),
                    ProfessionalProfile.VerificationStatus.APPROVED: professional_status.get(
                        ProfessionalProfile.VerificationStatus.APPROVED,
                        0,
                    ),
                    ProfessionalProfile.VerificationStatus.REJECTED: professional_status.get(
                        ProfessionalProfile.VerificationStatus.REJECTED,
                        0,
                    ),
                    ProfessionalProfile.VerificationStatus.NEEDS_CORRECTION: professional_status.get(
                        ProfessionalProfile.VerificationStatus.NEEDS_CORRECTION,
                        0,
                    ),
                    ProfessionalProfile.VerificationStatus.SUSPENDED: professional_status.get(
                        ProfessionalProfile.VerificationStatus.SUSPENDED,
                        0,
                    ),
                },
                "verification_queue": {
                    "submitted_total": ProfessionalVerificationSubmission.objects.filter(
                        status=ProfessionalVerificationSubmission.Status.SUBMITTED
                    ).count(),
                    "under_review_total": ProfessionalVerificationSubmission.objects.filter(
                        status=ProfessionalVerificationSubmission.Status.UNDER_REVIEW
                    ).count(),
                    "needs_correction_total": ProfessionalVerificationSubmission.objects.filter(
                        status=ProfessionalVerificationSubmission.Status.NEEDS_CORRECTION
                    ).count(),
                    "pending_documents_total": ProfessionalDocument.objects.filter(
                        review_status=ProfessionalDocument.ReviewStatus.PENDING
                    ).count(),
                    "rejected_documents_total": ProfessionalDocument.objects.filter(
                        review_status=ProfessionalDocument.ReviewStatus.REJECTED
                    ).count(),
                },
            },
            "appointments": {
                "total": Appointment.objects.count(),
                "today_total": Appointment.objects.filter(
                    scheduled_at__gte=today_start,
                    scheduled_at__lt=tomorrow_start,
                ).count(),
                "upcoming_7_days_total": Appointment.objects.filter(
                    scheduled_at__gte=now,
                    scheduled_at__lt=next_7_days,
                ).count(),
                "pending_confirmation_total": Appointment.objects.filter(
                    status=Appointment.Status.PENDING_CONFIRMATION
                ).count(),
                "confirmed_total": Appointment.objects.filter(
                    status=Appointment.Status.CONFIRMED
                ).count(),
                "unpaid_active_total": Appointment.objects.filter(
                    status__in=ACTIVE_APPOINTMENT_STATUSES,
                    is_paid=False,
                ).count(),
            },
            "payments": {
                "total": Payment.objects.count(),
                "pending_total": payment_stats["pending_total"] or 0,
                "succeeded_total": payment_stats["succeeded_total"] or 0,
                "failed_total": payment_stats["failed_total"] or 0,
                "pending_amount_total": str(payment_stats["pending_amount_total"] or 0),
                "succeeded_amount_total": str(
                    payment_stats["succeeded_amount_total"] or 0
                ),
                "failed_amount_total": str(payment_stats["failed_amount_total"] or 0),
            },
            "audits": {
                "total": AuditEvent.objects.count(),
                "events_last_24h": AuditEvent.objects.filter(
                    created_at__gte=now - timedelta(hours=24)
                ).count(),
                "events_last_7d": AuditEvent.objects.filter(
                    created_at__gte=now - timedelta(days=7)
                ).count(),
                "latest_event_at": latest_audit["latest_created_at"].isoformat()
                if latest_audit["latest_created_at"]
                else None,
            },
        },
        status=200,
    )


@require_http_methods(["GET"])
@api_roles_required("admin", "super_admin", "privacy_auditor")
def admin_dashboard_recent_activity_view(request):
    try:
        limit = _parse_positive_int(
            request.GET.get("limit"),
            default=20,
            field_name="limit",
            min_value=1,
            max_value=100,
        )
    except ValueError as exc:
        return _json_error(str(exc), status=400)

    items = list(
        AuditEvent.objects.select_related("actor")
        .all()
        .order_by("-created_at", "-id")[:limit]
    )

    return JsonResponse(
        {
            "items": [_serialize_event(event) for event in items],
            "limit": limit,
        },
        status=200,
    )
# FIN CAMBIO NUEVO
```

---

##### Archivo: `backend/apps/audits/api_urls.py`

```python
# INICIO CAMBIO NUEVO
from django.urls import path

from .api_views import (
    admin_audit_logs_collection_view,
    admin_audit_log_detail_view,
    admin_audit_logs_export_view,
    admin_dashboard_recent_activity_view,
    admin_dashboard_summary_view,
    my_audit_events_view,
)

urlpatterns = [
    path(
        "admin/audits/access-logs",
        admin_audit_logs_collection_view,
        name="admin-audit-logs-collection",
    ),
    path(
        "admin/audits/access-logs/export.csv",
        admin_audit_logs_export_view,
        name="admin-audit-logs-export",
    ),
    path(
        "admin/audits/access-logs/<int:event_id>",
        admin_audit_log_detail_view,
        name="admin-audit-logs-detail",
    ),
    path(
        "users/me/audit-events",
        my_audit_events_view,
        name="users-me-audit-events",
    ),
    path(
        "admin/dashboard/summary",
        admin_dashboard_summary_view,
        name="admin-dashboard-summary",
    ),
    path(
        "admin/dashboard/recent-activity",
        admin_dashboard_recent_activity_view,
        name="admin-dashboard-recent-activity",
    ),
]
# FIN CAMBIO NUEVO
```

---

##### Archivo: `backend/config/urls.py`

```python
# INICIO CAMBIO NUEVO
from django.contrib import admin
from django.urls import include, path

urlpatterns = [
    path("admin/", admin.site.urls),
    path("api/v1/", include("apps.users.api_urls")),
    path("api/v1/", include("apps.appointments.api_urls")),
    path("api/v1/", include("apps.professionals.api_urls")),
    path("api/v1/", include("apps.payments.api_urls")),
    path("api/v1/", include("apps.audits.api_urls")),
]
# FIN CAMBIO NUEVO
```

**Cómo probar esta fase**

Primero:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py runserver
```

Login admin:

```bash
curl -X POST http://127.0.0.1:8000/api/v1/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"admin@demo.local\",\"password\":\"Admin123!\"}"
```

Listar auditoría:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs?page=1&page_size=20" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Ver detalle:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs/1" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Exportar CSV:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/audits/access-logs/export.csv?limit=100" ^
  -H "Authorization: Bearer TOKEN_ADMIN" ^
  --output audit_logs.csv
```

Ver auditoría propia:

```bash
curl "http://127.0.0.1:8000/api/v1/users/me/audit-events" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Resumen del panel:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/dashboard/summary" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

Actividad reciente:

```bash
curl "http://127.0.0.1:8000/api/v1/admin/dashboard/recent-activity?limit=10" ^
  -H "Authorization: Bearer TOKEN_ADMIN"
```

**Qué debe seguir funcionando**

* login;
* `auth/me`;
* registro de paciente;
* registro de profesional;
* disponibilidad;
* reserva y cancelación de citas;
* flujo de verificación;
* pagos demo.

**Cómo revertir esta fase**

1. Eliminar:

   * `backend/apps/audits/api_views.py`
   * `backend/apps/audits/api_urls.py`

2. Restaurar `backend/config/urls.py`:

```powershell
copy /Y backend\config\urls.py.bak backend\config\urls.py
```

3. Validar otra vez:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
```

---

#### Prueba de Fase 1

Se considera correcta si se cumple esto:

* admin, super_admin y privacy_auditor pueden consultar logs y resumen;
* professional y patient no pueden entrar a rutas admin;
* export.csv descarga archivo;
* `users/me/audit-events` solo devuelve eventos del usuario autenticado;
* `dashboard/summary` devuelve métricas sin error aunque no existan muchos datos.

---

#### Fase 2

**Objetivo**

Dejar pruebas automáticas para que futuras modificaciones no rompan el panel administrativo.

**Riesgo controlado**

Se reduce el riesgo de romper permisos, filtros, exportación y resumen.

**Archivo a tocar**

`backend/apps/audits/tests_api.py` → **agregar archivo nuevo**

---

##### Archivo: `backend/apps/audits/tests_api.py`

```python
# INICIO CAMBIO NUEVO
import json
from datetime import timedelta

from django.contrib.auth import get_user_model
from django.test import TestCase
from django.utils import timezone

from apps.appointments.models import Appointment
from apps.catalogs.models import Specialty
from apps.patients.models import PatientProfile
from apps.payments.models import Payment
from apps.professionals.models import (
    ProfessionalDocument,
    ProfessionalProfile,
    ProfessionalVerificationSubmission,
)
from .models import AuditEvent


class AuditAndDashboardApiTests(TestCase):
    def setUp(self):
        User = get_user_model()

        self.admin_user = User.objects.create_user(
            email="admin.audit@test.local",
            password="Admin123!",
            first_name="Admin",
            last_name="Audit",
            role="admin",
            is_staff=True,
        )
        self.privacy_user = User.objects.create_user(
            email="privacy.audit@test.local",
            password="Privacy123!",
            first_name="Privacy",
            last_name="Audit",
            role="privacy_auditor",
        )
        self.professional_user = User.objects.create_user(
            email="professional.audit@test.local",
            password="Profesional123!",
            first_name="Doctor",
            last_name="Audit",
            role="professional",
        )
        self.patient_user = User.objects.create_user(
            email="patient.audit@test.local",
            password="Paciente123!",
            first_name="Paciente",
            last_name="Audit",
            role="patient",
        )

        specialty = Specialty.objects.create(name="Cardiología")

        self.professional_profile = ProfessionalProfile.objects.create(
            user=self.professional_user,
            specialty=specialty,
            license_number="LIC-100",
            city="Quito",
            province="Pichincha",
            verification_status=ProfessionalProfile.VerificationStatus.SUBMITTED,
            public_profile_enabled=True,
            is_accepting_patients=True,
            consultation_fee="30.00",
            teleconsultation_fee="25.00",
        )
        self.patient_profile = PatientProfile.objects.create(
            user=self.patient_user,
            national_id="1712345678",
        )

        ProfessionalDocument.objects.create(
            professional=self.professional_profile,
            document_type=ProfessionalDocument.DocumentType.TITLE,
            file_path="/tmp/title.pdf",
            original_name="title.pdf",
            review_status=ProfessionalDocument.ReviewStatus.PENDING,
        )
        ProfessionalVerificationSubmission.objects.create(
            professional=self.professional_profile,
            status=ProfessionalVerificationSubmission.Status.SUBMITTED,
            assigned_admin=self.admin_user,
        )

        now = timezone.now()
        self.appointment = Appointment.objects.create(
            patient=self.patient_profile,
            professional=self.professional_profile,
            scheduled_at=now + timedelta(hours=3),
            ends_at=now + timedelta(hours=4),
            modality=Appointment.Modality.IN_PERSON,
            status=Appointment.Status.PENDING_CONFIRMATION,
            price="30.00",
            is_paid=False,
        )
        self.payment = Payment.objects.create(
            appointment=self.appointment,
            external_reference="demo_reference_001",
            amount="30.00",
            currency="USD",
            status=Payment.Status.PENDING,
        )

        self.event_admin = AuditEvent.objects.create(
            actor=self.admin_user,
            event_type="payment_marked_succeeded",
            entity_type="Payment",
            entity_id=str(self.payment.id),
            metadata={"status": "pending"},
        )
        self.event_patient = AuditEvent.objects.create(
            actor=self.patient_user,
            event_type="payment_intent_created",
            entity_type="Payment",
            entity_id=str(self.payment.id),
            metadata={"appointment_id": self.appointment.id},
        )
        self.event_professional = AuditEvent.objects.create(
            actor=self.professional_user,
            event_type="professional_verification_submitted",
            entity_type="ProfessionalVerificationSubmission",
            entity_id="1",
            metadata={"professional_id": self.professional_profile.id},
        )

        self.admin_token = self._login("admin.audit@test.local", "Admin123!")
        self.privacy_token = self._login("privacy.audit@test.local", "Privacy123!")
        self.prof_token = self._login(
            "professional.audit@test.local",
            "Profesional123!",
        )
        self.patient_token = self._login("patient.audit@test.local", "Paciente123!")

    def _login(self, email, password):
        response = self.client.post(
            "/api/v1/auth/login",
            data=json.dumps({"email": email, "password": password}),
            content_type="application/json",
        )
        self.assertEqual(response.status_code, 200)
        return response.json()["access_token"]

    def _auth_headers(self, token):
        return {"HTTP_AUTHORIZATION": f"Bearer {token}"}

    def test_admin_can_list_audit_logs(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertGreaterEqual(len(response.json()["items"]), 3)

    def test_admin_can_filter_audit_logs(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs?event_type=payment_intent_created",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        items = response.json()["items"]
        self.assertEqual(len(items), 1)
        self.assertEqual(items[0]["event_type"], "payment_intent_created")

    def test_admin_can_view_audit_log_detail(self):
        response = self.client.get(
            f"/api/v1/admin/audits/access-logs/{self.event_patient.id}",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertEqual(response.json()["id"], self.event_patient.id)

    def test_admin_can_export_audit_logs_csv(self):
        response = self.client.get(
            "/api/v1/admin/audits/access-logs/export.csv?limit=2",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertIn("text/csv", response["Content-Type"])
        self.assertEqual(response["X-Export-Row-Limit"], "2")

    def test_user_can_only_see_own_audit_events(self):
        response = self.client.get(
            "/api/v1/users/me/audit-events",
            **self._auth_headers(self.patient_token),
        )
        self.assertEqual(response.status_code, 200)
        items = response.json()["items"]
        self.assertEqual(len(items), 1)
        self.assertEqual(items[0]["actor"]["email"], "patient.audit@test.local")

    def test_admin_can_get_dashboard_summary(self):
        response = self.client.get(
            "/api/v1/admin/dashboard/summary",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        payload = response.json()

        self.assertEqual(payload["users"]["by_role"]["admin"], 1)
        self.assertEqual(payload["users"]["by_role"]["professional"], 1)
        self.assertEqual(payload["users"]["by_role"]["patient"], 1)
        self.assertEqual(
            payload["professionals"]["verification_queue"]["submitted_total"],
            1,
        )
        self.assertEqual(payload["payments"]["pending_total"], 1)
        self.assertEqual(payload["appointments"]["pending_confirmation_total"], 1)

    def test_privacy_auditor_can_get_dashboard_summary(self):
        response = self.client.get(
            "/api/v1/admin/dashboard/summary",
            **self._auth_headers(self.privacy_token),
        )
        self.assertEqual(response.status_code, 200)

    def test_professional_cannot_get_dashboard_summary(self):
        response = self.client.get(
            "/api/v1/admin/dashboard/summary",
            **self._auth_headers(self.prof_token),
        )
        self.assertEqual(response.status_code, 403)

    def test_admin_can_get_recent_activity(self):
        response = self.client.get(
            "/api/v1/admin/dashboard/recent-activity?limit=2",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 200)
        self.assertEqual(response.json()["limit"], 2)
        self.assertEqual(len(response.json()["items"]), 2)

    def test_recent_activity_limit_validation(self):
        response = self.client.get(
            "/api/v1/admin/dashboard/recent-activity?limit=999",
            **self._auth_headers(self.admin_token),
        )
        self.assertEqual(response.status_code, 400)
# FIN CAMBIO NUEVO
```

**Cómo probar esta fase**

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py test apps.audits.tests_api -v 2
```

**Qué debe seguir funcionando**

* autenticación;
* disponibilidad;
* agenda;
* verificación profesional;
* pagos demo.

**Cómo revertir esta fase**

Eliminar:

* `backend/apps/audits/tests_api.py`

No toca base de datos.

---

#### Prueba de Fase 2

La fase queda correcta si:

* el test de auditoría pasa completo;
* los roles están bien restringidos;
* el resumen responde;
* la actividad reciente respeta `limit`;
* el usuario común no accede a rutas admin.

---

#### Pruebas de regresión

Después de pegar todo, el programador debe correr esto:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.appointments.tests_api_booking -v 2
python manage.py test apps.professionals.tests_api -v 2
python manage.py test apps.professionals.tests_api_admin -v 2
python manage.py test apps.payments.tests_api -v 2
python manage.py test apps.audits.tests_api -v 2
```

Rutas nuevas que deben quedar activas:

* `GET /api/v1/admin/audits/access-logs`
* `GET /api/v1/admin/audits/access-logs/<id>`
* `GET /api/v1/admin/audits/access-logs/export.csv`
* `GET /api/v1/users/me/audit-events`
* `GET /api/v1/admin/dashboard/summary`
* `GET /api/v1/admin/dashboard/recent-activity`

---

#### Plan de reversión total

1. Eliminar:

   * `backend/apps/audits/api_views.py`
   * `backend/apps/audits/api_urls.py`
   * `backend/apps/audits/tests_api.py`

2. Restaurar el archivo original:

```powershell
copy /Y backend\config\urls.py.bak backend\config\urls.py
```

3. Validar integridad:

```bash
cd G:\codex_projects\ubemedicos\backend
python manage.py check
```

4. Ejecutar regresión mínima:

```bash
python manage.py test apps.users.tests_api -v 2
python manage.py test apps.appointments.tests_api -v 2
python manage.py test apps.professionals.tests_api -v 2
python manage.py test apps.payments.tests_api -v 2
```

Con este bloque, el backend ya queda listo para que el frontend consuma un **panel administrativo/privacidad real** sin depender de datos inventados.
